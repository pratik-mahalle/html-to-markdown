// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import html_to_markdown

import gleam/list
import gleam/string

// Empty HTML document
pub fn empty_html_test() {
  let result = html_to_markdown.convert("<html><head></head><body></body></html>")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// CJK (Chinese, Japanese, Korean) characters are preserved
pub fn encoding_cjk_characters_test() {
  let result = html_to_markdown.convert("<p>中文内容</p><p>日本語テキスト</p><p>한국어 텍스트</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("中文内容") |> should.equal(True)
  result.content |> string.contains("日本語テキスト") |> should.equal(True)
  result.content |> string.contains("한국어 텍스트") |> should.equal(True)
}

// Common HTML entities are decoded in output
pub fn encoding_html_entities_test() {
  let result = html_to_markdown.convert("<p>&amp; &lt; &gt; &nbsp; &quot; &apos;</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("&") |> should.equal(True)
  result.content |> string.contains("<") |> should.equal(True)
  result.content |> string.contains(">") |> should.equal(True)
}

// Named HTML entities like &mdash; and &hellip; are decoded
pub fn encoding_named_entities_test() {
  let result = html_to_markdown.convert("<p>Em dash&mdash;used for parenthetical remarks&mdash;is common. Ellipsis&hellip; indicates omission. Non-breaking&nbsp;space.</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("—") |> should.equal(True)
  result.content |> string.contains("…") |> should.equal(True)
}

// Numeric HTML entities (decimal and hex) are decoded
pub fn encoding_numeric_entities_test() {
  let result = html_to_markdown.convert("<p>Copyright: &#169; Trade: &#174; Euro: &#8364; Hex: &#x00A9;</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("©") |> should.equal(True)
  result.content |> string.contains("®") |> should.equal(True)
  result.content |> string.contains("€") |> should.equal(True)
}

// Emoji and Unicode characters are preserved
pub fn encoding_unicode_emoji_test() {
  let result = html_to_markdown.convert("<p>Hello 🌍 World 🚀</p><p>Stars: ⭐ ✨</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("🌍") |> should.equal(True)
  result.content |> string.contains("🚀") |> should.equal(True)
  result.content |> string.contains("⭐") |> should.equal(True)
}

// Document containing only HTML comments produces empty output
pub fn html_comments_only_test() {
  let result = html_to_markdown.convert("<!-- This is a comment --><!-- Another comment -->")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// Input that is only whitespace characters (spaces, tabs, newlines) produces empty output
pub fn just_whitespace_input_test() {
  let result = html_to_markdown.convert("   ")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// Deeply nested elements (100 levels) are handled without stack overflow
pub fn malformed_deeply_nested_elements_test() {
  let result = html_to_markdown.convert("<div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><p>Deeply nested content</p></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Deeply nested content") |> should.equal(True)
}

// Missing closing tags on block elements are auto-closed by parser
pub fn malformed_missing_block_closing_tags_test() {
  let result = html_to_markdown.convert("<div><h1>Title<p>First paragraph<p>Second paragraph</div>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Title") |> should.equal(True)
  result.content |> string.contains("First paragraph") |> should.equal(True)
  result.content |> string.contains("Second paragraph") |> should.equal(True)
}

// Overlapping bold/italic tags are recovered by the HTML parser without panic
pub fn malformed_overlapping_tags_test() {
  let result = html_to_markdown.convert("<p><b><i>bold and italic</b></i></p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("bold and italic") |> should.equal(True)
}

// Unclosed <p> tag is recovered gracefully and content is preserved
pub fn malformed_unclosed_paragraph_test() {
  let result = html_to_markdown.convert("<p>This paragraph is never closed")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("This paragraph is never closed") |> should.equal(True)
}

// Document with only script tags produces empty output (scripts are stripped)
pub fn script_tags_only_test() {
  let result = html_to_markdown.convert("<html><head><script>alert('xss')</script></head><body><script>document.write('hello')</script></body></html>")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// Document with only style tags produces empty output (styles are stripped)
pub fn style_tags_only_test() {
  let result = html_to_markdown.convert("<html><head><style>body { color: red; }</style></head><body><style>.foo { margin: 0; }</style></body></html>")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// Visitor handles custom elements with nested content
pub fn visitor_custom_element_with_nesting_test() {
  let result = html_to_markdown.convert("<div><custom-widget data-value=\"123\"><p>Widget content here</p><span>With nested elements</span></custom-widget></div>")
  result |> should.be_ok()
  result.content |> string.contains("[CUSTOM WIDGET]") |> should.equal(True)
  result.content |> string.contains("Widget content here") |> should.equal(False)
}

// Visitor skips deeply nested elements
pub fn visitor_deeply_nested_skip_test() {
  let result = html_to_markdown.convert("<div><p>Outer <em>emphasis <strong>with bold <mark>and highlight</mark></strong></em> text</p></div>")
  result |> should.be_ok()
  result.content |> string.contains("Outer") |> should.equal(True)
  result.content |> string.contains("text") |> should.equal(True)
  result.content |> string.contains("highlight") |> should.equal(False)
}

// Visitor modifies element at end after children processed
pub fn visitor_element_end_modification_test() {
  let result = html_to_markdown.convert("<blockquote><p>Original quote</p></blockquote>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
}

// Visitor skips at element_start level removes entire subtree
pub fn visitor_element_start_skip_entire_subtree_test() {
  let result = html_to_markdown.convert("<div><h1>Title</h1><p>Content</p></div>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
}

// Visitor preserves unknown HTML tags as raw HTML
pub fn visitor_unknown_tag_preservation_test() {
  let result = html_to_markdown.convert("<article><p>Article text</p><x-custom>Custom element with content</x-custom><p>More article text</p></article>")
  result |> should.be_ok()
  result.content |> string.contains("Article text") |> should.equal(True)
  result.content |> string.contains("More article text") |> should.equal(True)
  result.content |> string.contains("<x-custom>") |> should.equal(True)
}

// Whitespace-only content
pub fn whitespace_only_test() {
  let result = html_to_markdown.convert("<p>   </p>")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// onclick and other on* event handlers are removed from elements
pub fn xss_onclick_handler_removed_test() {
  let result = html_to_markdown.convert("<p><a href=\"https://example.com\" onclick=\"alert('xss')\">Click me</a></p><button onmouseover=\"steal_data()\">Hover me</button>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Click me") |> should.equal(True)
}

// Script tag content is stripped and does not appear in output
pub fn xss_script_tag_stripped_test() {
  let result = html_to_markdown.convert("<p>Safe content.</p><script>alert('xss')</script><p>More safe content.</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Safe content") |> should.equal(True)
  result.content |> string.contains("More safe content") |> should.equal(True)
}

// Script tags nested inside SVG are stripped
pub fn xss_svg_nested_script_stripped_test() {
  let result = html_to_markdown.convert("<p>Before SVG.</p><svg xmlns=\"http://www.w3.org/2000/svg\"><script>alert('svg-xss')</script><text>SVG text</text></svg><p>After SVG.</p>")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Before SVG") |> should.equal(True)
  result.content |> string.contains("After SVG") |> should.equal(True)
}

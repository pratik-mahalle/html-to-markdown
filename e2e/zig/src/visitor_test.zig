// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// E2e tests for category: visitor

test "visitor_audio_custom" {
    // Visitor replaces audio element with custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[AUDIO: podcast.mp3]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Listen to this:") != null);
}

test "visitor_audio_skip" {
    // Visitor removes audio elements from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Background music:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Enjoy!") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "music.ogg") == null);
}

test "visitor_button_custom" {
    // Visitor replaces button with bracketed text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[BTN:Click me]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "[BTN:Cancel]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Confirm action:") != null);
}

test "visitor_button_skip" {
    // Visitor removes all buttons from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Actions available:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Save") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Delete") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Export") == null);
}

test "visitor_continue_default" {
    // Visitor continue action preserves default conversion
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Hello <strong>World</strong></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "**World**") != null);
}

test "visitor_custom_blockquote" {
    // Visitor replaces blockquote with custom format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<blockquote><p>A wise quote.</p></blockquote>");
    try testing.expect(std.mem.indexOf(u8, result.content, "QUOTE:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "A wise quote.") != null);
}

test "visitor_custom_emphasis" {
    // Visitor replaces emphasis with custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>This is <em>important</em> text.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, ">>>important<<<") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "*important*") == null);
}

test "visitor_custom_heading" {
    // Visitor replaces heading with custom format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h2>Section Title</h2><p>Content below heading.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "--- Section Title ---") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "## Section Title") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Content below heading.") != null);
}

test "visitor_custom_image" {
    // Visitor replaces image with custom output using template
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<img src=\"banner.png\" alt=\"Banner\">");
    try testing.expect(std.mem.indexOf(u8, result.content, "[Image: Banner]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "banner.png") == null);
}

test "visitor_custom_link_format" {
    // Visitor reformats links using template interpolation
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Example (https://example.com)") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "[Example]") == null);
}

test "visitor_custom_link_static" {
    // Visitor replaces link with static custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\">Click here</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[REDACTED LINK]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "example.com") == null);
}

test "visitor_custom_output" {
    // Visitor custom action replaces element output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h1>Original Heading</h1>");
    try testing.expect(std.mem.indexOf(u8, result.content, "## REPLACED HEADING") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "# Original Heading") == null);
}

test "visitor_definition_list_custom" {
    // Visitor customizes definition list items
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>");
    try testing.expect(std.mem.indexOf(u8, result.content, "**HTML**") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "**CSS**") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "HyperText Markup Language") != null);
}

test "visitor_definition_list_custom_format" {
    // Visitor formats definition lists with custom templates
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>");
    try testing.expect(std.mem.indexOf(u8, result.content, "### Python") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "### JavaScript") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "> A high-level programming language") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "> A scripting language for web browsers") != null);
}

test "visitor_definition_list_skip" {
    // Visitor skips definition list items from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Glossary:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "End of glossary") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Term A") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Definition") == null);
}

test "visitor_details_summary_custom" {
    // Visitor customizes details/summary disclosure elements
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[EXPANDABLE] Click to expand") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "This content is initially hidden.") != null);
}

test "visitor_details_summary_skip" {
    // Visitor removes details/summary elements entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Main content here.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "More main content.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Hidden section") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Secret details") == null);
}

test "visitor_figure_custom" {
    // Visitor customizes figure and figcaption elements
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Article Title") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "*Figure 1: System Architecture*") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Explanation of the figure.") != null);
}

test "visitor_figure_custom_wrap" {
    // Visitor wraps figure content with custom formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[FIGURE]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "[/FIGURE]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Gallery") != null);
}

test "visitor_figure_skip" {
    // Visitor removes figure elements with their captions
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "See the chart below:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "As shown in the chart above.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Revenue Trends") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "chart.svg") == null);
}

test "visitor_form_custom" {
    // Visitor replaces form with custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[FORM PLACEHOLDER]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "submit") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "input") == null);
}

test "visitor_form_skip" {
    // Visitor skips form elements entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Before form") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "After form") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "email") == null);
}

test "visitor_horizontal_rule_custom" {
    // Visitor replaces horizontal rule with custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[DIVIDER]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Section A") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Section B") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "---") == null);
}

test "visitor_horizontal_rule_skip" {
    // Visitor removes all horizontal rules
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Part 1") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Part 2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Part 3") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "---") == null);
}

test "visitor_iframe_custom" {
    // Visitor replaces embedded iframe with custom text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[EMBEDDED: https://maps.example.com/embed]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Embedded map:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "End of map") != null);
}

test "visitor_iframe_skip" {
    // Visitor removes embedded iframes
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Reviews") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "See reviews from our partners.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "widget.example.com") == null);
}

test "visitor_input_custom" {
    // Visitor replaces input with labeled output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[INPUT:text]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "[INPUT:password]") != null);
}

test "visitor_input_skip" {
    // Visitor skips all input elements
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Sign up:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Continue") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "email") == null);
}

test "visitor_line_break_custom" {
    // Visitor replaces line break with custom output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>First line<br>Second line<br>Third line</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "First line | Second line | Third line") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "\n\n") == null);
}

test "visitor_line_break_skip" {
    // Visitor removes all line breaks
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Address Line 1Address Line 2Address Line 3") != null);
}

test "visitor_mark_custom" {
    // Visitor replaces highlight/mark with custom template
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>This is a <mark>highlighted passage</mark> in the text.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "==highlighted passage==") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "This is a") != null);
}

test "visitor_mark_skip" {
    // Visitor skips mark elements entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Key insight: <mark>always validate input</mark> for security.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "always validate input") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Key insight:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "for security.") != null);
}

test "visitor_preserve_html" {
    // Visitor preserve_html action includes raw HTML in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<div><custom-tag>Custom content</custom-tag></div>");
    try testing.expect(std.mem.indexOf(u8, result.content, "<custom-tag>") != null);
}

test "visitor_skip_all_headings" {
    // Visitor skips all headings from document
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h1>Title</h1><p>Body text remains.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Title") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Body text remains.") != null);
}

test "visitor_skip_code_blocks" {
    // Visitor skips code blocks from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Intro text") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Outro text") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "let x = 42") == null);
}

test "visitor_skip_heading" {
    // Visitor skip action omits all headings from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h1>Title</h1><p>Body text remains.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Title") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Body text remains.") != null);
}

test "visitor_skip_images" {
    // Visitor skips all images from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Before image") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "After image") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "photo.jpg") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "A photo") == null);
}

test "visitor_skip_links" {
    // Visitor skips all links entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Before <a href=\"https://example.com\">link text</a> after</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "link text") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "example.com") == null);
}

test "visitor_skip_strong" {
    // Visitor skips bold/strong elements
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Normal <strong>bold text</strong> normal</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "bold text") == null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Normal") != null);
}

test "visitor_subscript_custom" {
    // Visitor replaces subscript with custom template
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>H<sub>2</sub>O is water.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "H~2~O") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "is water") != null);
}

test "visitor_subscript_skip" {
    // Visitor skips subscript elements entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "The formula CHO is sugar.") != null);
}

test "visitor_superscript_custom" {
    // Visitor replaces superscript with custom template
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "E=mc^2^") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "revolutionized physics") != null);
}

test "visitor_superscript_skip" {
    // Visitor skips superscript from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "The equation x + y = z has no solutions.") != null);
}

test "visitor_underline_custom" {
    // Visitor replaces underline with custom markup
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>This is <u>very important</u> text.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "_very important_") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "**") == null);
}

test "visitor_underline_skip" {
    // Visitor skips underline elements from output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Normal text with <u>underlined part</u> and more text.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Normal text with") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "and more text.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "underlined part") == null);
}

test "visitor_video_custom" {
    // Visitor replaces video with custom link
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[VIDEO: tutorial.mp4]") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Watch our tutorial:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Great content!") != null);
}

test "visitor_video_skip" {
    // Visitor removes video elements entirely
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Demo") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "See the demo above.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "demo.webm") == null);
}

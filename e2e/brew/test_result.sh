#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: result
set -euo pipefail

test_result_tables_empty_when_no_tables() {
  # Result tables array is empty when input has no tables
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_tables
  count_tables=$(echo "$output" | jq '.tables | length')
  [ "$count_tables" -eq 0 ] || exit 1
}

test_result_tables_multiple() {
  # Multiple tables each appear in the tables array
  local output
  output=$(html_to_markdown convert)

  local count_tables
  count_tables=$(echo "$output" | jq '.tables | length')
  assert_count_min "$count_tables" 2 'tables'
}

test_result_tables_simple() {
  # Simple table populates the tables array in result
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_tables
  count_tables=$(echo "$output" | jq '.tables | length')
  assert_count_min "$count_tables" 1 'tables'
}

test_result_tables_without_structure_flag() {
  # Tables array is empty when includeDocumentStructure is false
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_tables
  count_tables=$(echo "$output" | jq '.tables | length')
  [ "$count_tables" -eq 0 ] || exit 1
}

test_result_warnings_empty_for_clean_input() {
  # Warnings array is empty for well-formed HTML without problematic content
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_warnings
  count_warnings=$(echo "$output" | jq '.warnings | length')
  [ "$count_warnings" -eq 0 ] || exit 1
}

test_result_warnings_empty_for_complex_input() {
  # Warnings array is empty for complex but valid HTML
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_warnings
  count_warnings=$(echo "$output" | jq '.warnings | length')
  [ "$count_warnings" -eq 0 ] || exit 1
}

test_result_warnings_empty_for_malformed_html() {
  # Warnings array is empty even for malformed HTML (parser is lenient)
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local count_warnings
  count_warnings=$(echo "$output" | jq '.warnings | length')
  [ "$count_warnings" -eq 0 ] || exit 1
}

run_tests_result() {
  run_test test_result_tables_empty_when_no_tables
  run_test test_result_tables_multiple
  run_test test_result_tables_simple
  run_test test_result_tables_without_structure_flag
  run_test test_result_warnings_empty_for_clean_input
  run_test test_result_warnings_empty_for_complex_input
  run_test test_result_warnings_empty_for_malformed_html
}

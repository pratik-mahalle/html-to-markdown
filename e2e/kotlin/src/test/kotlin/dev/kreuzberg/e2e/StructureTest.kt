// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: structure. */
class StructureTest {
    @Test
    fun testStructureCodeBlock() {
        // Fenced code block produces Code node
        val result = HtmlToMarkdownRs.convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureDeepNestingH1H2H3() {
        // H1 > H2 > H3 creates three levels of heading nesting
        val result = HtmlToMarkdownRs.convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 5, "expected at least 5 elements")
    }

    @Test
    fun testStructureH1H2NestedGroup() {
        // H1 followed by H2 creates a nested group under the H1
        val result = HtmlToMarkdownRs.convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 3, "expected at least 3 elements")
    }

    @Test
    fun testStructureHeadingParagraph() {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        val result = HtmlToMarkdownRs.convert("<h1>Title</h1><p>A paragraph of text.</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureList() {
        // Unordered list produces List and ListItem nodes
        val result = HtmlToMarkdownRs.convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testStructureMultipleHeadings() {
        // Multiple headings create multiple Heading nodes with correct levels
        val result = HtmlToMarkdownRs.convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 4, "expected at least 4 elements")
    }

    @Test
    fun testStructureSiblingH1Groups() {
        // H1, H2, then another H1 creates two sibling top-level groups
        val result = HtmlToMarkdownRs.convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertFalse(result.document.nodes.isEmpty(), "expected non-empty value")
        assertTrue(result.document.nodes.size >= 4, "expected at least 4 elements")
    }

}

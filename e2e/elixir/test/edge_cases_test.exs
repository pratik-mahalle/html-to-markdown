# E2e tests for category: edge-cases
# Generated by html-to-markdown-e2e-generator — do not edit manually.

defmodule HtmlToMarkdown.EdgeCasesTest do
  use ExUnit.Case

  test "empty_html: Empty HTML document" do
    html = "<html><head></head><body></body></html>"
    {:ok, result} = HtmlToMarkdown.convert(html)
    content = result[:content] || ""

    assert String.trim(content) == ""
  end

  test "whitespace_only: Whitespace-only content" do
    html = "<p>   </p>"
    {:ok, result} = HtmlToMarkdown.convert(html)
    content = result[:content] || ""

    assert String.trim(content) == ""
  end
end

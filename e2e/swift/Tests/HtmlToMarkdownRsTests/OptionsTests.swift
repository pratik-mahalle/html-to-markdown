// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import HtmlToMarkdownRs

/// E2e tests for category: options.
final class OptionsTests: XCTestCase {
    func testOptionsAutolinksFalse() throws {
        // Bare URL links rendered as regular markdown links when autolinks disabled
        let result = try convert("<p><a href='https://example.com'>https://example.com</a></p>", "{\"autolinks\":false}")
        XCTAssertTrue(result.content.contains("example.com"), "expected to contain: \("example.com")")
    }

    func testOptionsBrInTablesFalse() throws {
        // BR elements in table cells are stripped when disabled
        let result = try convert("<table><tr><th>Col</th></tr><tr><td>A<br>B</td></tr></table>", "{\"brInTables\":false}")
        XCTAssertTrue(result.content.contains("Col"), "expected to contain: \("Col")")
    }

    func testOptionsBrInTablesTrue() throws {
        // BR elements in table cells render as line breaks
        let result = try convert("<table><tr><th>Header</th></tr><tr><td>Line 1<br>Line 2</td></tr></table>", "{\"brInTables\":true}")
        XCTAssertTrue(result.content.contains("Header"), "expected to contain: \("Header")")
        XCTAssertTrue(result.content.contains("Line 1"), "expected to contain: \("Line 1")")
        XCTAssertTrue(result.content.contains("Line 2"), "expected to contain: \("Line 2")")
    }

    func testOptionsCodeBlockBackticks() throws {
        // Backticks code block style uses triple backtick fences
        let result = try convert("<pre><code class=\"language-js\">console.log('hi');</code></pre>", "{\"codeBlockStyle\":\"Backticks\"}")
        XCTAssertTrue(result.content.contains("```"), "expected to contain: \("```")")
        XCTAssertTrue(result.content.contains("console.log('hi');"), "expected to contain: \("console.log('hi');")")
    }

    func testOptionsCodeBlockIndented() throws {
        // Code blocks use 4-space indentation
        let result = try convert("<pre><code>print('hello')</code></pre>", "{\"codeBlockStyle\":\"Indented\"}")
        XCTAssertTrue(result.content.contains("print('hello')"), "expected to contain: \("print('hello')")")
        XCTAssertFalse(result.content.contains("```"), "expected NOT to contain: \("```")")
    }

    func testOptionsCodeBlockTildes() throws {
        // Code blocks use tilde fences
        let result = try convert("<pre><code>let x = 1;</code></pre>", "{\"codeBlockStyle\":\"Tildes\"}")
        XCTAssertTrue(result.content.contains("~~~"), "expected to contain: \("~~~")")
        XCTAssertTrue(result.content.contains("let x = 1;"), "expected to contain: \("let x = 1;")")
    }

    func testOptionsCodeBlockTildesStyle() throws {
        // Tildes code block style uses triple tilde fences
        let result = try convert("<pre><code>some code</code></pre>", "{\"codeBlockStyle\":\"Tildes\"}")
        XCTAssertTrue(result.content.contains("~~~"), "expected to contain: \("~~~")")
        XCTAssertTrue(result.content.contains("some code"), "expected to contain: \("some code")")
    }

    func testOptionsCodeLanguagePython() throws {
        // Default code language annotation on blocks without lang attribute
        let result = try convert("<pre><code>def hello(): pass</code></pre>", "{\"codeLanguage\":\"python\"}")
        XCTAssertTrue(result.content.contains("```python"), "expected to contain: \("```python")")
        XCTAssertTrue(result.content.contains("def hello"), "expected to contain: \("def hello")")
    }

    func testOptionsConvertAsInline() throws {
        // Block elements treated as inline
        let result = try convert("<p>One</p><p>Two</p>", "{\"convertAsInline\":true}")
        XCTAssertTrue(result.content.contains("One"), "expected to contain: \("One")")
        XCTAssertTrue(result.content.contains("Two"), "expected to contain: \("Two")")
    }

    func testOptionsDebugTrue() throws {
        // Debug mode enabled does not crash and produces output
        let result = try convert("<p>Debug test</p>", "{\"debug\":true}")
        XCTAssertTrue(result.content.contains("Debug test"), "expected to contain: \("Debug test")")
    }

    func testOptionsDefaultTitleTrue() throws {
        // Links without title get empty title attribute when defaultTitle is true
        let result = try convert("<p><a href='https://example.com'>Link</a></p>", "{\"defaultTitle\":true}")
        XCTAssertTrue(result.content.contains("Link"), "expected to contain: \("Link")")
        XCTAssertTrue(result.content.contains("https://example.com"), "expected to contain: \("https://example.com")")
    }

    func testOptionsEncodingUtf8() throws {
        // UTF-8 encoding hint for special characters
        let result = try convert("<p>Café naïve résumé</p>", "{\"encoding\":\"utf-8\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testOptionsEscapeAsciiEnabled() throws {
        // ASCII Markdown characters are escaped when escapeAscii is true
        let result = try convert("<p>Text with # hash and [brackets] and * star</p>", "{\"escapeAscii\":true}")
        XCTAssertTrue(result.content.contains("Text"), "expected to contain: \("Text")")
        XCTAssertTrue(result.content.contains("hash"), "expected to contain: \("hash")")
        XCTAssertTrue(result.content.contains("brackets"), "expected to contain: \("brackets")")
        XCTAssertTrue(result.content.contains("star"), "expected to contain: \("star")")
    }

    func testOptionsEscapeAsterisks() throws {
        // escape_asterisks option escapes asterisks in plain text
        let result = try convert("<p>Use 2*3 = 6 in math.</p>", "{\"escapeAsterisks\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("2"), "expected to contain: \("2")")
        XCTAssertTrue(result.content.contains("3"), "expected to contain: \("3")")
        XCTAssertTrue(result.content.contains("6"), "expected to contain: \("6")")
    }

    func testOptionsEscapeMisc() throws {
        // escape_misc option escapes miscellaneous markdown characters
        let result = try convert("<p>Use # and | and ~ in text.</p>", "{\"escapeMisc\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Use"), "expected to contain: \("Use")")
        XCTAssertTrue(result.content.contains("and"), "expected to contain: \("and")")
        XCTAssertTrue(result.content.contains("in text."), "expected to contain: \("in text.")")
    }

    func testOptionsEscapeUnderscores() throws {
        // escape_underscores option escapes underscores in plain text
        let result = try convert("<p>The variable_name is defined.</p>", "{\"escapeUnderscores\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("variable"), "expected to contain: \("variable")")
        XCTAssertTrue(result.content.contains("name"), "expected to contain: \("name")")
        XCTAssertTrue(result.content.contains("defined."), "expected to contain: \("defined.")")
    }

    func testOptionsExcludeSelectorsAttribute() throws {
        // Elements matching CSS attribute selector are excluded entirely
        let result = try convert("<body><div role=\"complementary\">Sidebar</div><p>Primary text</p></body>", "{\"excludeSelectors\":[\"[role='complementary']\"]}")
        XCTAssertTrue(result.content.contains("Primary text"), "expected to contain: \("Primary text")")
        XCTAssertFalse(result.content.contains("Sidebar"), "expected NOT to contain: \("Sidebar")")
    }

    func testOptionsExcludeSelectorsClass() throws {
        // Elements matching CSS class selector are excluded entirely
        let result = try convert("<body><div class=\"cookie-banner\">Accept cookies</div><p>Main content</p></body>", "{\"excludeSelectors\":[\".cookie-banner\"]}")
        XCTAssertTrue(result.content.contains("Main content"), "expected to contain: \("Main content")")
        XCTAssertFalse(result.content.contains("cookies"), "expected NOT to contain: \("cookies")")
    }

    func testOptionsExcludeSelectorsEmptyNoop() throws {
        // Empty exclude_selectors list does not affect output
        let result = try convert("<p>Hello world</p>", "{\"excludeSelectors\":[]}")
        XCTAssertTrue(result.content.contains("Hello world"), "expected to contain: \("Hello world")")
    }

    func testOptionsExcludeSelectorsId() throws {
        // Elements matching CSS id selector are excluded entirely
        let result = try convert("<body><div id=\"ad-container\">Buy stuff</div><p>Article text</p></body>", "{\"excludeSelectors\":[\"#ad-container\"]}")
        XCTAssertTrue(result.content.contains("Article text"), "expected to contain: \("Article text")")
        XCTAssertFalse(result.content.contains("Buy stuff"), "expected NOT to contain: \("Buy stuff")")
    }

    func testOptionsExcludeSelectorsMultiple() throws {
        // Multiple CSS selectors each exclude their matched elements
        let result = try convert("<body><nav class=\"nav\">Menu</nav><p>Content</p><footer>Footer</footer></body>", "{\"excludeSelectors\":[\".nav\",\"footer\"]}")
        XCTAssertTrue(result.content.contains("Content"), "expected to contain: \("Content")")
        XCTAssertFalse(result.content.contains("Menu"), "expected NOT to contain: \("Menu")")
        XCTAssertFalse(result.content.contains("Footer"), "expected NOT to contain: \("Footer")")
    }

    func testOptionsExcludeSelectorsNestedContentDropped() throws {
        // All descendants of excluded elements are dropped
        let result = try convert("<body><aside class=\"sidebar\"><h2>Related</h2><p>Sidebar text</p></aside><main><p>Main text</p></main></body>", "{\"excludeSelectors\":[\".sidebar\"]}")
        XCTAssertTrue(result.content.contains("Main text"), "expected to contain: \("Main text")")
        XCTAssertFalse(result.content.contains("Related"), "expected NOT to contain: \("Related")")
        XCTAssertFalse(result.content.contains("Sidebar text"), "expected NOT to contain: \("Sidebar text")")
    }

    func testOptionsExcludeSelectorsPlainTextMode() throws {
        // Exclude selectors work in plain text output mode
        let result = try convert("<body><div class=\"nav\">Navigation</div><p>Article body</p></body>", "{\"excludeSelectors\":[\".nav\"],\"outputFormat\":\"Plain\"}")
        XCTAssertTrue(result.content.contains("Article body"), "expected to contain: \("Article body")")
        XCTAssertFalse(result.content.contains("Navigation"), "expected NOT to contain: \("Navigation")")
    }

    func testOptionsExcludeSelectorsVsStripTags() throws {
        // exclude_selectors drops entire subtree unlike strip_tags which keeps children
        let result = try convert("<body><div class=\"wrapper\"><p>Inner paragraph</p></div><p>Outer text</p></body>", "{\"excludeSelectors\":[\".wrapper\"]}")
        XCTAssertTrue(result.content.contains("Outer text"), "expected to contain: \("Outer text")")
        XCTAssertFalse(result.content.contains("Inner paragraph"), "expected NOT to contain: \("Inner paragraph")")
    }

    func testOptionsExtractMetadataTrue() throws {
        // Extract metadata returns document metadata when enabled
        let result = try convert("<html><head><title>Test Page</title><meta name='description' content='A test page'></head><body><p>Content</p></body></html>", "{\"extractMetadata\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertEqual(result.metadata.document.title.trimmingCharacters(in: .whitespaces), "Test Page")
        XCTAssertEqual(result.metadata.document.description.trimmingCharacters(in: .whitespaces), "A test page")
    }

    func testOptionsHeadingStyleAtx() throws {
        // ATX heading style produces hash-prefixed headings
        let result = try convert("<h1>Title</h1><h2>Subtitle</h2>", "{\"headingStyle\":\"Atx\"}")
        XCTAssertTrue(result.content.contains("# Title"), "expected to contain: \("# Title")")
        XCTAssertTrue(result.content.contains("## Subtitle"), "expected to contain: \("## Subtitle")")
    }

    func testOptionsHeadingStyleAtxClosed() throws {
        // ATX closed heading style adds closing hashes
        let result = try convert("<h1>Closed Heading</h1>", "{\"headingStyle\":\"AtxClosed\"}")
        XCTAssertTrue(result.content.contains("# Closed Heading #"), "expected to contain: \("# Closed Heading #")")
    }

    func testOptionsHeadingStyleUnderlined() throws {
        // Underlined heading style produces setext-style headings for h1 and h2
        let result = try convert("<h1>Main Title</h1>", "{\"headingStyle\":\"Underlined\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Main Title"), "expected to contain: \("Main Title")")
    }

    func testOptionsHighlightBold() throws {
        // Mark tag rendered as bold
        let result = try convert("<p>Text with <mark>highlighted</mark> text.</p>", "{\"highlightStyle\":\"Bold\"}")
        XCTAssertTrue(result.content.contains("**highlighted**"), "expected to contain: \("**highlighted**")")
    }

    func testOptionsHighlightDoubleEqual() throws {
        // Mark tag with double equal highlight style
        let result = try convert("<p>Text with <mark>highlighted</mark> here.</p>", "{\"highlightStyle\":\"DoubleEqual\"}")
        XCTAssertTrue(result.content.contains("==highlighted=="), "expected to contain: \("==highlighted==")")
    }

    func testOptionsHighlightNone() throws {
        // Mark tag with no highlight style strips the mark
        let result = try convert("<p>Text with <mark>plain</mark> content.</p>", "{\"highlightStyle\":\"None\"}")
        XCTAssertTrue(result.content.contains("plain"), "expected to contain: \("plain")")
        XCTAssertFalse(result.content.contains("=="), "expected NOT to contain: \("==")")
    }

    func testOptionsKeepInlineImagesInParagraph() throws {
        // Images inside specified tags stay inline
        let result = try convert("<p>Text <img src='icon.png' alt='icon'> more text</p>", "{\"keepInlineImagesIn\":[\"p\"]}")
        XCTAssertTrue(result.content.contains("Text"), "expected to contain: \("Text")")
        XCTAssertTrue(result.content.contains("more text"), "expected to contain: \("more text")")
    }

    func testOptionsLinkStyleReference() throws {
        // Links use reference-style formatting
        let result = try convert("<p><a href='https://example.com'>Example</a> and <a href='https://other.com'>Other</a></p>", "{\"linkStyle\":\"Reference\"}")
        XCTAssertTrue(result.content.contains("Example"), "expected to contain: \("Example")")
        XCTAssertTrue(result.content.contains("Other"), "expected to contain: \("Other")")
        XCTAssertTrue(result.content.contains("example.com"), "expected to contain: \("example.com")")
    }

    func testOptionsListCustomBullets() throws {
        // Custom bullet character for unordered lists
        let result = try convert("<ul><li>Item A</li><li>Item B</li></ul>", "{\"bullets\":\"*\"}")
        XCTAssertTrue(result.content.contains("* Item A"), "expected to contain: \("* Item A")")
        XCTAssertTrue(result.content.contains("* Item B"), "expected to contain: \("* Item B")")
    }

    func testOptionsListIndentTabs() throws {
        // Tab indentation type for nested list items
        let result = try convert("<ul><li>Parent<ul><li>Child</li></ul></li></ul>", "{\"listIndentType\":\"Tabs\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Parent"), "expected to contain: \("Parent")")
        XCTAssertTrue(result.content.contains("Child"), "expected to contain: \("Child")")
    }

    func testOptionsListIndentWidthFour() throws {
        // Nested lists indented with 4 spaces per level
        let result = try convert("<ul><li>Outer<ul><li>Inner</li></ul></li></ul>", "{\"listIndentWidth\":4}")
        XCTAssertTrue(result.content.contains("Outer"), "expected to contain: \("Outer")")
        XCTAssertTrue(result.content.contains("Inner"), "expected to contain: \("Inner")")
    }

    func testOptionsMaxDepthDefaultUnlimited() throws {
        // Default max_depth (null) converts deeply nested content fully
        let result = try convert("<div><div><div><div><p>Deep content</p></div></div></div></div>")
        XCTAssertTrue(result.content.contains("Deep content"), "expected to contain: \("Deep content")")
    }

    func testOptionsMaxDepthTruncates() throws {
        // max_depth truncates content beyond the specified depth
        let result = try convert("<div><p>Shallow</p><div><div><div><p>Too deep</p></div></div></div></div>", "{\"maxDepth\":3}")
        XCTAssertTrue(result.content.contains("Shallow"), "expected to contain: \("Shallow")")
        XCTAssertFalse(result.content.contains("Too deep"), "expected NOT to contain: \("Too deep")")
    }

    func testOptionsMaxDepthZeroEmpty() throws {
        // max_depth of 0 produces empty output
        let result = try convert("<p>Hello</p>", "{\"maxDepth\":0}")
        XCTAssertEqual(result.content.trimmingCharacters(in: .whitespaces), "")
    }

    func testOptionsNewlineBackslash() throws {
        // Hard line breaks rendered with backslash
        let result = try convert("<p>Line one<br>Line two</p>", "{\"newlineStyle\":\"Backslash\"}")
        XCTAssertTrue(result.content.contains("Line one"), "expected to contain: \("Line one")")
        XCTAssertTrue(result.content.contains("Line two"), "expected to contain: \("Line two")")
    }

    func testOptionsNewlineSpaces() throws {
        // Hard line breaks rendered with trailing spaces
        let result = try convert("<p>First<br>Second</p>", "{\"newlineStyle\":\"Spaces\"}")
        XCTAssertTrue(result.content.contains("First"), "expected to contain: \("First")")
        XCTAssertTrue(result.content.contains("Second"), "expected to contain: \("Second")")
    }

    func testOptionsOutputFormatDjot() throws {
        // Djot output format produces djot-compatible markup
        let result = try convert("<p>Simple paragraph.</p>", "{\"outputFormat\":\"Djot\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Simple paragraph."), "expected to contain: \("Simple paragraph.")")
    }

    func testOptionsOutputFormatMarkdown() throws {
        // Default markdown output format produces standard markdown
        let result = try convert("<h1>Title</h1><p>Some text.</p>", "{\"headingStyle\":\"Atx\",\"outputFormat\":\"Markdown\"}")
        XCTAssertTrue(result.content.contains("# Title"), "expected to contain: \("# Title")")
        XCTAssertTrue(result.content.contains("Some text."), "expected to contain: \("Some text.")")
    }

    func testOptionsOutputFormatPlain() throws {
        // Plain text output format strips markdown syntax
        let result = try convert("<h1>Title</h1><p>Some <strong>bold</strong> text.</p>", "{\"outputFormat\":\"Plain\"}")
        XCTAssertTrue(result.content.contains("Title"), "expected to contain: \("Title")")
        XCTAssertTrue(result.content.contains("bold"), "expected to contain: \("bold")")
        XCTAssertTrue(result.content.contains("text."), "expected to contain: \("text.")")
    }

    func testOptionsPreprocessingAggressive() throws {
        // Aggressive preset removes nav, footer, aside unconditionally
        let result = try convert("<nav>Menu</nav><article><h1>Title</h1><p>Content</p></article><aside>Sidebar</aside><footer>Footer</footer>", "{\"preprocessing\":{\"preset\":\"Aggressive\"}}")
        XCTAssertTrue(result.content.contains("Title"), "expected to contain: \("Title")")
        XCTAssertTrue(result.content.contains("Content"), "expected to contain: \("Content")")
        XCTAssertFalse(result.content.contains("Menu"), "expected NOT to contain: \("Menu")")
    }

    func testOptionsPreprocessingMinimal() throws {
        // Minimal preset preserves nav, footer, aside
        let result = try convert("<nav>Navigation</nav><p>Content</p><footer>Footer</footer>", "{\"preprocessing\":{\"preset\":\"Minimal\"}}")
        XCTAssertTrue(result.content.contains("Navigation"), "expected to contain: \("Navigation")")
        XCTAssertTrue(result.content.contains("Content"), "expected to contain: \("Content")")
        XCTAssertTrue(result.content.contains("Footer"), "expected to contain: \("Footer")")
    }

    func testOptionsPreprocessingRemoveForms() throws {
        // Forms are removed when remove_forms is true
        let result = try convert("<p>Before</p><form><input type='text'/><button>Submit</button></form><p>After</p>", "{\"preprocessing\":{\"removeForms\":true}}")
        XCTAssertTrue(result.content.contains("Before"), "expected to contain: \("Before")")
        XCTAssertTrue(result.content.contains("After"), "expected to contain: \("After")")
        XCTAssertFalse(result.content.contains("Submit"), "expected NOT to contain: \("Submit")")
    }

    func testOptionsPreserveTagsIframe() throws {
        // Iframe tags preserved as raw HTML in output
        let result = try convert("<p>Before</p><iframe src='video.html' width='560'></iframe><p>After</p>", "{\"preserveTags\":[\"iframe\"]}")
        XCTAssertTrue(result.content.contains("Before"), "expected to contain: \("Before")")
        XCTAssertTrue(result.content.contains("After"), "expected to contain: \("After")")
        XCTAssertTrue(result.content.contains("<iframe"), "expected to contain: \("<iframe")")
    }

    func testOptionsSkipImagesTrue() throws {
        // Images are omitted from output when skipImages is true
        let result = try convert("<p>Before <img src='test.jpg' alt='photo'> After</p>", "{\"skipImages\":true}")
        XCTAssertTrue(result.content.contains("Before"), "expected to contain: \("Before")")
        XCTAssertTrue(result.content.contains("After"), "expected to contain: \("After")")
        XCTAssertFalse(result.content.contains("photo"), "expected NOT to contain: \("photo")")
    }

    func testOptionsStripNewlines() throws {
        // Strip newlines produces single-line paragraphs
        let result = try convert("<p>First paragraph.</p><p>Second paragraph.</p>", "{\"stripNewlines\":true}")
        XCTAssertTrue(result.content.contains("First paragraph."), "expected to contain: \("First paragraph.")")
        XCTAssertTrue(result.content.contains("Second paragraph."), "expected to contain: \("Second paragraph.")")
    }

    func testOptionsStripTagsDivSpan() throws {
        // Div and span tags stripped but content preserved
        let result = try convert("<div class='wrapper'><p>Inside div</p></div><p>Outside <span class='hl'>span text</span></p>", "{\"stripTags\":[\"div\",\"span\"]}")
        XCTAssertTrue(result.content.contains("Inside div"), "expected to contain: \("Inside div")")
        XCTAssertTrue(result.content.contains("span text"), "expected to contain: \("span text")")
    }

    func testOptionsStrongEmUnderscore() throws {
        // Strong and em tags use underscore symbol instead of asterisk
        let result = try convert("<p><strong>bold</strong> and <em>italic</em></p>", "{\"strongEmSymbol\":\"_\"}")
        XCTAssertTrue(result.content.contains("__bold__"), "expected to contain: \("__bold__")")
        XCTAssertTrue(result.content.contains("_italic_"), "expected to contain: \("_italic_")")
    }

    func testOptionsSubSymbolTilde() throws {
        // Subscript rendered with tilde symbol
        let result = try convert("<p>H<sub>2</sub>O</p>", "{\"subSymbol\":\"~\"}")
        XCTAssertTrue(result.content.contains("~2~"), "expected to contain: \("~2~")")
    }

    func testOptionsSupSymbolCaret() throws {
        // Superscript rendered with caret symbol
        let result = try convert("<p>x<sup>2</sup></p>", "{\"supSymbol\":\"^\"}")
        XCTAssertTrue(result.content.contains("^2^"), "expected to contain: \("^2^")")
    }

    func testOptionsWhitespaceNormalized() throws {
        // Normalized whitespace mode collapses multiple spaces
        let result = try convert("<p>Text   with    extra   spaces.</p>", "{\"whitespaceMode\":\"Normalized\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Text"), "expected to contain: \("Text")")
        XCTAssertTrue(result.content.contains("with"), "expected to contain: \("with")")
        XCTAssertTrue(result.content.contains("extra"), "expected to contain: \("extra")")
        XCTAssertTrue(result.content.contains("spaces."), "expected to contain: \("spaces.")")
    }

    func testOptionsWhitespaceStrict() throws {
        // Strict whitespace mode preserves whitespace as-is
        let result = try convert("<p>Preserved   spacing.</p>", "{\"whitespaceMode\":\"Strict\"}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("Preserved"), "expected to contain: \("Preserved")")
        XCTAssertTrue(result.content.contains("spacing."), "expected to contain: \("spacing.")")
    }

    func testOptionsWrapDisabled() throws {
        // Wrap option disabled preserves long lines without breaking
        let result = try convert("<p>This is a long paragraph that should not be wrapped at all because wrapping is disabled.</p>", "{\"wrap\":false}")
        XCTAssertTrue(result.content.contains("This is a long paragraph that should not be wrapped at all because wrapping is disabled."), "expected to contain: \("This is a long paragraph that should not be wrapped at all because wrapping is disabled.")")
    }

    func testOptionsWrapEnabled() throws {
        // Wrap option enabled with custom width wraps long lines
        let result = try convert("<p>This is a long paragraph that should be wrapped at the specified column width when the wrap option is enabled.</p>", "{\"wrap\":true,\"wrapWidth\":40}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertTrue(result.content.contains("This is a long paragraph"), "expected to contain: \("This is a long paragraph")")
    }

}

/**
 * E2e tests for category: conversion
 * Generated by html-to-markdown-e2e-generator — do not edit manually.
 */

import { describe, it, expect } from 'vitest';
import { convert } from '@kreuzberg/html-to-markdown-wasm';

describe('conversion', () => {
  it('blockquote_multiple_paragraphs: Blockquote with multiple paragraphs has each paragraph prefixed', () => {
    const html = `<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('> First paragraph.');
    expect(content).toContain('> Second paragraph.');
  });

  it('blockquote_nested: Nested blockquote produces double-prefixed lines', () => {
    const html = `<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Outer quote.');
    expect(content).toContain('Inner quote.');
  });

  it('blockquote_simple: Simple blockquote', () => {
    const html = `<blockquote><p>Quote text</p></blockquote>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('> Quote text');
  });

  it('blockquote_with_list: Blockquote containing a list preserves list items inside quote', () => {
    const html = `<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Quote intro:');
    expect(content).toContain('Point one');
    expect(content).toContain('Point two');
  });

  it('bold_and_italic: Nested bold and italic', () => {
    const html = `<p><strong><em>both</em></strong></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('***both***');
  });

  it('bold_strong: Strong tag converts to bold', () => {
    const html = `<p><strong>bold</strong></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('**bold**');
  });

  it('code_block: Code block with language preserves content', () => {
    const html = `<pre><code class="language-python">print('hello')</code></pre>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('print('hello')');
  });

  it('code_block_no_language: Code block without a language class preserves content', () => {
    const html = `<pre><code>plain code here</code></pre>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('plain code here');
  });

  it('code_inline_in_paragraph: Inline code element nested inside a paragraph', () => {
    const html = `<p>Call the <code>initialize()</code> method first.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('\`initialize()\`');
  });

  it('code_with_backticks_in_content: Inline code containing backtick characters is properly escaped', () => {
    const html = `<p>Use <code>\`backtick\` here</code> carefully.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('backtick');
  });

  it('emphasis_mark_highlight: mark tag produces highlighted output', () => {
    const html = `<p><mark>highlighted</mark></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('highlighted');
  });

  it('emphasis_strikethrough_del: del tag converts to GFM strikethrough', () => {
    const html = `<p><del>deleted text</del></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('~~deleted text~~');
  });

  it('emphasis_strikethrough_s: s tag converts to GFM strikethrough', () => {
    const html = `<p><s>strikethrough</s></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('~~strikethrough~~');
  });

  it('emphasis_subscript: sub tag content is preserved', () => {
    const html = `<p>H<sub>2</sub>O</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('H');
    expect(content).toContain('2');
    expect(content).toContain('O');
  });

  it('emphasis_superscript: sup tag content is preserved', () => {
    const html = `<p>x<sup>2</sup></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('x');
    expect(content).toContain('2');
  });

  it('emphasis_underline_u: u tag content is preserved in output', () => {
    const html = `<p><u>underlined</u></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('underlined');
  });

  it('form_input_elements: Form input elements produce readable output without form mechanics', () => {
    const html = `<form><label for="name">Name:</label><input type="text" id="name" placeholder="Enter name"></form>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Name');
  });

  it('form_select_options: Select element with options produces readable output', () => {
    const html = `<form><label>Color:</label><select><option value="red">Red</option><option value="blue" selected>Blue</option><option value="green">Green</option></select></form>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Color');
  });

  it('form_textarea: Textarea element produces readable output', () => {
    const html = `<form><label>Message:</label><textarea>Default text content</textarea></form>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Message');
  });

  it('heading_h1: H1 heading', () => {
    const html = `<h1>Heading 1</h1>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('# Heading 1');
  });

  it('heading_h2: H2 heading', () => {
    const html = `<h2>Heading 2</h2>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('## Heading 2');
  });

  it('heading_h3: H3 heading', () => {
    const html = `<h3>Heading 3</h3>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('### Heading 3');
  });

  it('heading_h4: H4 heading', () => {
    const html = `<h4>Heading 4</h4>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('#### Heading 4');
  });

  it('heading_h5: H5 heading', () => {
    const html = `<h5>Heading 5</h5>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('##### Heading 5');
  });

  it('heading_h6: H6 heading', () => {
    const html = `<h6>Heading 6</h6>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('###### Heading 6');
  });

  it('image_figure_figcaption: Figure with figcaption preserves both image and caption', () => {
    const html = `<figure><img src="sunset.jpg" alt="A sunset"><figcaption>Beautiful sunset over the ocean</figcaption></figure>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('![A sunset](sunset.jpg)');
    expect(content).toContain('Beautiful sunset over the ocean');
  });

  it('image_linked: Image inside an anchor produces a linked image', () => {
    const html = `<a href="https://example.com"><img src="icon.png" alt="Icon"></a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('![Icon](icon.png)');
    expect(content).toContain('https://example.com');
  });

  it('image_no_alt: Image without alt text produces image markdown', () => {
    const html = `<img src="banner.jpg">`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('banner.jpg');
  });

  it('image_simple: Image with alt text', () => {
    const html = `<img src="photo.jpg" alt="A photo">`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('![A photo](photo.jpg)');
  });

  it('image_with_title: Image with title attribute includes title in output', () => {
    const html = `<img src="chart.png" alt="Sales chart" title="Q3 Sales">`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('![Sales chart](chart.png');
    expect(content).toContain('Q3 Sales');
  });

  it('inline_code: Inline code', () => {
    const html = `<p>Use <code>console.log()</code> to debug</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('\`console.log()\`');
  });

  it('italic_em: Em tag converts to italic', () => {
    const html = `<p><em>italic</em></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('*italic*');
  });

  it('line_break_br_tag: Single br tag produces a line break in output', () => {
    const html = `<p>First line.<br>Second line.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('First line.');
    expect(content).toContain('Second line.');
  });

  it('line_break_hr_tag: hr tag produces a horizontal separator between content', () => {
    const html = `<p>Before rule.</p><hr><p>After rule.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Before rule.');
    expect(content).toContain('After rule.');
  });

  it('line_break_multiple_br: Multiple consecutive br tags in sequence', () => {
    const html = `<p>Start.<br><br>End.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Start.');
    expect(content).toContain('End.');
  });

  it('link_anchor_fragment: Fragment-only anchor link is preserved', () => {
    const html = `<a href="#section">Jump to section</a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('[Jump to section](#section)');
  });

  it('link_empty_href: Link with empty href produces output with the link text', () => {
    const html = `<a href="">No destination</a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('No destination');
  });

  it('link_image_inside: Image inside a link produces a linked image', () => {
    const html = `<a href="https://example.com"><img src="logo.png" alt="Logo"></a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('![Logo](logo.png)');
    expect(content).toContain('https://example.com');
  });

  it('link_mailto: Mailto link is preserved with mailto: scheme', () => {
    const html = `<a href="mailto:user@example.com">Email us</a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('mailto:user@example.com');
  });

  it('link_simple: Simple link', () => {
    const html = `<a href="https://example.com">Example</a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('[Example](https://example.com)');
  });

  it('link_with_bold_text: Link containing bold text preserves formatting', () => {
    const html = `<a href="https://example.com"><strong>Bold link</strong></a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('**Bold link**');
    expect(content).toContain('https://example.com');
  });

  it('link_with_title: Link with title attribute', () => {
    const html = `<a href="https://example.com" title="Example Site">Example</a>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('[Example](https://example.com');
    expect(content).toContain('Example Site');
  });

  it('list_definition_dl: Definition list with dt and dd elements', () => {
    const html = `<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Term One');
    expect(content).toContain('Definition of term one.');
    expect(content).toContain('Term Two');
    expect(content).toContain('Definition of term two.');
  });

  it('list_item_multiple_paragraphs: List item containing multiple paragraphs', () => {
    const html = `<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('First paragraph in item.');
    expect(content).toContain('Second paragraph in item.');
    expect(content).toContain('Simple item');
  });

  it('list_mixed_nested: Mixed list: ordered list nested inside unordered list', () => {
    const html = `<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Item A');
    expect(content).toContain('Sub 1');
    expect(content).toContain('Sub 2');
    expect(content).toContain('Item B');
  });

  it('list_nested_ordered: Nested ordered list with two levels of depth', () => {
    const html = `<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Step 1');
    expect(content).toContain('Step 1a');
    expect(content).toContain('Step 1b');
    expect(content).toContain('Step 2');
  });

  it('list_nested_unordered: Nested unordered list with two levels of depth', () => {
    const html = `<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Parent A');
    expect(content).toContain('Child A1');
    expect(content).toContain('Child A2');
    expect(content).toContain('Parent B');
  });

  it('list_task_checkboxes: Task list with checked and unchecked checkboxes', () => {
    const html = `<ul><li><input type="checkbox" checked> Done task</li><li><input type="checkbox"> Pending task</li></ul>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Done task');
    expect(content).toContain('Pending task');
  });

  it('ordered_list: Ordered list', () => {
    const html = `<ol><li>First</li><li>Second</li><li>Third</li></ol>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('1. First');
    expect(content).toContain('2. Second');
    expect(content).toContain('3. Third');
  });

  it('paragraph_multiple: Multiple paragraphs are separated by a blank line', () => {
    const html = `<p>First paragraph.</p><p>Second paragraph.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('First paragraph.');
    expect(content).toContain('Second paragraph.');
  });

  it('paragraph_nested_divs: Text nested inside divs is extracted correctly', () => {
    const html = `<div><div><p>Nested text</p></div></div>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Nested text');
  });

  it('paragraph_simple: Simple paragraph converts to plain text', () => {
    const html = `<p>Hello World</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('Hello World');
  });

  it('paragraph_with_inline_formatting: Paragraph with bold, italic, and a link', () => {
    const html = `<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href="https://example.com">link</a>.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('**bold**');
    expect(content).toContain('*italic*');
    expect(content).toContain('[link](https://example.com)');
  });

  it('paragraph_with_line_breaks: Paragraph with br tags produces line breaks in output', () => {
    const html = `<p>Line one.<br>Line two.<br>Line three.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Line one.');
    expect(content).toContain('Line two.');
    expect(content).toContain('Line three.');
  });

  it('semantic_abbr: Abbreviation element text is preserved', () => {
    const html = `<p>The <abbr title="World Wide Web">WWW</abbr> is global.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('WWW');
  });

  it('semantic_article: Article element wrapping content preserves inner content', () => {
    const html = `<article><h2>Article Title</h2><p>Article body.</p></article>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Article Title');
    expect(content).toContain('Article body.');
  });

  it('semantic_definition_list: Definition list with term and description', () => {
    const html = `<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('HTML');
    expect(content).toContain('HyperText Markup Language');
    expect(content).toContain('CSS');
    expect(content).toContain('Cascading Style Sheets');
  });

  it('semantic_details_summary: Details and summary elements produce readable output', () => {
    const html = `<details><summary>Click to expand</summary><p>Hidden content here.</p></details>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Click to expand');
  });

  it('semantic_hr: Horizontal rule produces a separator in output', () => {
    const html = `<p>Above</p><hr><p>Below</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Above');
    expect(content).toContain('Below');
  });

  it('semantic_mark_highlight: Mark tag produces highlighted output', () => {
    const html = `<p>This is <mark>highlighted text</mark> in a sentence.</p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('highlighted text');
  });

  it('semantic_section_with_heading: Section element with heading preserves structure', () => {
    const html = `<section><h3>Section Heading</h3><p>Section content.</p></section>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Section Heading');
    expect(content).toContain('Section content.');
  });

  it('semantic_sub_superscript: Subscript and superscript elements are preserved in output', () => {
    const html = `<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('H');
    expect(content).toContain('2');
    expect(content).toContain('O');
    expect(content).toContain('E=mc');
  });

  it('simple_table: Simple table with header', () => {
    const html = `<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('Name');
    expect(content).toContain('Age');
    expect(content).toContain('Alice');
    expect(content).toContain('30');
    expect(content).toContain('|');
    expect(content).toContain('---');
  });

  it('table_empty: Empty table produces no output or minimal output', () => {
    const html = `<table></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim()).toBe('');
  });

  it('table_no_thead: Table without thead uses first row as implied header', () => {
    const html = `<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Product');
    expect(content).toContain('Price');
    expect(content).toContain('Apple');
    expect(content).toContain('1.00');
    expect(content).toContain('|');
  });

  it('table_pipe_chars_in_content: Table cells containing pipe characters are escaped in output', () => {
    const html = `<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Expression');
    expect(content).toContain('Result');
    expect(content).toContain('true');
  });

  it('table_with_alignment: Table with column alignment attributes', () => {
    const html = `<table><thead><tr><th align="left">Left</th><th align="center">Center</th><th align="right">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Left');
    expect(content).toContain('Center');
    expect(content).toContain('Right');
    expect(content).toContain('L');
    expect(content).toContain('C');
    expect(content).toContain('R');
    expect(content).toContain('|');
  });

  it('table_with_colspan: Table with colspan attribute in a header cell', () => {
    const html = `<table><thead><tr><th colspan="2">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content.trim().length).toBeGreaterThan(0);
    expect(content).toContain('Full Name');
    expect(content).toContain('John');
    expect(content).toContain('Doe');
  });

  it('unordered_list: Unordered list', () => {
    const html = `<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>`;
    const result = convert(html);
    const content = result.content ?? '';

    expect(content).toContain('- Item 1');
    expect(content).toContain('- Item 2');
    expect(content).toContain('- Item 3');
  });

});

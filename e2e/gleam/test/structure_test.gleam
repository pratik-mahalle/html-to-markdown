// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import html_to_markdown

import gleam/list

// Fenced code block produces Code node
pub fn structure_code_block_test() {
  let result = html_to_markdown.convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(2) |> should.equal(True)
}

// H1 > H2 > H3 creates three levels of heading nesting
pub fn structure_deep_nesting_h1_h2_h3_test() {
  let result = html_to_markdown.convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(5) |> should.equal(True)
}

// H1 followed by H2 creates a nested group under the H1
pub fn structure_h1_h2_nested_group_test() {
  let result = html_to_markdown.convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(3) |> should.equal(True)
}

// Simple heading followed by paragraph produces Heading and Paragraph nodes
pub fn structure_heading_paragraph_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><p>A paragraph of text.</p>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(2) |> should.equal(True)
}

// Unordered list produces List and ListItem nodes
pub fn structure_list_test() {
  let result = html_to_markdown.convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(2) |> should.equal(True)
}

// Multiple headings create multiple Heading nodes with correct levels
pub fn structure_multiple_headings_test() {
  let result = html_to_markdown.convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(4) |> should.equal(True)
}

// H1, H2, then another H1 creates two sibling top-level groups
pub fn structure_sibling_h1_groups_test() {
  let result = html_to_markdown.convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", "{\"includeDocumentStructure\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.is_empty |> should.equal(False)
  result.document.nodes |> list.length |> int.is_at_least(4) |> should.equal(True)
}

/*! E2e tests for category: conversion
 *  Generated by html-to-markdown-e2e-generator — do not edit manually.
 */

#include "html_to_markdown.h"
#include <assert.h>
#include <stdio.h>
#include <string.h>

void test_blockquote_simple(void) {
    /* Simple blockquote */
    const char *html = "<blockquote><p>Quote text</p></blockquote>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "> Quote text") != NULL);
    html_to_markdown_free_string(result);
}

void test_bold_and_italic(void) {
    /* Nested bold and italic */
    const char *html = "<p><strong><em>both</em></strong></p>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "***both***") != NULL);
    html_to_markdown_free_string(result);
}

void test_bold_strong(void) {
    /* Strong tag converts to bold */
    const char *html = "<p><strong>bold</strong></p>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "**bold**") != NULL);
    html_to_markdown_free_string(result);
}

void test_code_block(void) {
    /* Code block with language */
    const char *html = "<pre><code class=\"language-python\">print('hello')</code></pre>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "```python") != NULL);
    assert(strstr(result, "print('hello')") != NULL);
    assert(strstr(result, "```") != NULL);
    html_to_markdown_free_string(result);
}

void test_heading_h1(void) {
    /* H1 heading */
    const char *html = "<h1>Heading 1</h1>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "# Heading 1") == 0);
    html_to_markdown_free_string(result);
}

void test_heading_h2(void) {
    /* H2 heading */
    const char *html = "<h2>Heading 2</h2>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "## Heading 2") == 0);
    html_to_markdown_free_string(result);
}

void test_heading_h3(void) {
    /* H3 heading */
    const char *html = "<h3>Heading 3</h3>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "### Heading 3") == 0);
    html_to_markdown_free_string(result);
}

void test_heading_h4(void) {
    /* H4 heading */
    const char *html = "<h4>Heading 4</h4>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "#### Heading 4") == 0);
    html_to_markdown_free_string(result);
}

void test_heading_h5(void) {
    /* H5 heading */
    const char *html = "<h5>Heading 5</h5>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "##### Heading 5") == 0);
    html_to_markdown_free_string(result);
}

void test_heading_h6(void) {
    /* H6 heading */
    const char *html = "<h6>Heading 6</h6>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_equals */
    assert(strcmp(result, "###### Heading 6") == 0);
    html_to_markdown_free_string(result);
}

void test_image_simple(void) {
    /* Image with alt text */
    const char *html = "<img src=\"photo.jpg\" alt=\"A photo\">";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "![A photo](photo.jpg)") != NULL);
    html_to_markdown_free_string(result);
}

void test_inline_code(void) {
    /* Inline code */
    const char *html = "<p>Use <code>console.log()</code> to debug</p>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "`console.log()`") != NULL);
    html_to_markdown_free_string(result);
}

void test_italic_em(void) {
    /* Em tag converts to italic */
    const char *html = "<p><em>italic</em></p>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "*italic*") != NULL);
    html_to_markdown_free_string(result);
}

void test_link_simple(void) {
    /* Simple link */
    const char *html = "<a href=\"https://example.com\">Example</a>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "[Example](https://example.com)") != NULL);
    html_to_markdown_free_string(result);
}

void test_link_with_title(void) {
    /* Link with title attribute */
    const char *html = "<a href=\"https://example.com\" title=\"Example Site\">Example</a>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "[Example](https://example.com") != NULL);
    assert(strstr(result, "Example Site") != NULL);
    html_to_markdown_free_string(result);
}

void test_ordered_list(void) {
    /* Ordered list */
    const char *html = "<ol><li>First</li><li>Second</li><li>Third</li></ol>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "1. First") != NULL);
    assert(strstr(result, "2. Second") != NULL);
    assert(strstr(result, "3. Third") != NULL);
    html_to_markdown_free_string(result);
}

void test_simple_table(void) {
    /* Simple table with header */
    const char *html = "<table><thead><tr><th>Name</th><th>Age</th></tr></"
                       "thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "Name") != NULL);
    assert(strstr(result, "Age") != NULL);
    assert(strstr(result, "Alice") != NULL);
    assert(strstr(result, "30") != NULL);
    assert(strstr(result, "|") != NULL);
    assert(strstr(result, "---") != NULL);
    html_to_markdown_free_string(result);
}

void test_unordered_list(void) {
    /* Unordered list */
    const char *html = "<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>";
    char *result = html_to_markdown_convert(html, NULL);
    assert(result != NULL && "conversion should succeed");

    /* content_contains_all */
    assert(strstr(result, "- Item 1") != NULL);
    assert(strstr(result, "- Item 2") != NULL);
    assert(strstr(result, "- Item 3") != NULL);
    html_to_markdown_free_string(result);
}

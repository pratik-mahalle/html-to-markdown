// Code generated by html-to-markdown-e2e-generator; DO NOT EDIT.

// E2e tests for category: conversion
package dev.kreuzberg.e2e;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;
import dev.kreuzberg.htmltomarkdown.HtmlToMarkdown;
import dev.kreuzberg.htmltomarkdown.ConversionResult;

class ConversionTest {

    @Test
    void testBlockquoteSimple() {
        // Simple blockquote
        var html = "<blockquote><p>Quote text</p></blockquote>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("> Quote text"), "expected content to contain: > Quote text");
    }

    @Test
    void testBoldAndItalic() {
        // Nested bold and italic
        var html = "<p><strong><em>both</em></strong></p>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("***both***"), "expected content to contain: ***both***");
    }

    @Test
    void testBoldStrong() {
        // Strong tag converts to bold
        var html = "<p><strong>bold</strong></p>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("**bold**"), "expected content to contain: **bold**");
    }

    @Test
    void testCodeBlock() {
        // Code block with language
        var html = "<pre><code class=\"language-python\">print('hello')</code></pre>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("```python"), "expected content to contain: ```python");
        assertTrue(content.contains("print('hello')"), "expected content to contain: print('hello')");
        assertTrue(content.contains("```"), "expected content to contain: ```");
    }

    @Test
    void testHeadingH1() {
        // H1 heading
        var html = "<h1>Heading 1</h1>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("# Heading 1", content.strip(), "content_equals mismatch");
    }

    @Test
    void testHeadingH2() {
        // H2 heading
        var html = "<h2>Heading 2</h2>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("## Heading 2", content.strip(), "content_equals mismatch");
    }

    @Test
    void testHeadingH3() {
        // H3 heading
        var html = "<h3>Heading 3</h3>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("### Heading 3", content.strip(), "content_equals mismatch");
    }

    @Test
    void testHeadingH4() {
        // H4 heading
        var html = "<h4>Heading 4</h4>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("#### Heading 4", content.strip(), "content_equals mismatch");
    }

    @Test
    void testHeadingH5() {
        // H5 heading
        var html = "<h5>Heading 5</h5>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("##### Heading 5", content.strip(), "content_equals mismatch");
    }

    @Test
    void testHeadingH6() {
        // H6 heading
        var html = "<h6>Heading 6</h6>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertEquals("###### Heading 6", content.strip(), "content_equals mismatch");
    }

    @Test
    void testImageSimple() {
        // Image with alt text
        var html = "<img src=\"photo.jpg\" alt=\"A photo\">";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("![A photo](photo.jpg)"), "expected content to contain: ![A photo](photo.jpg)");
    }

    @Test
    void testInlineCode() {
        // Inline code
        var html = "<p>Use <code>console.log()</code> to debug</p>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("`console.log()`"), "expected content to contain: `console.log()`");
    }

    @Test
    void testItalicEm() {
        // Em tag converts to italic
        var html = "<p><em>italic</em></p>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("*italic*"), "expected content to contain: *italic*");
    }

    @Test
    void testLinkSimple() {
        // Simple link
        var html = "<a href=\"https://example.com\">Example</a>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("[Example](https://example.com)"), "expected content to contain: [Example](https://example.com)");
    }

    @Test
    void testLinkWithTitle() {
        // Link with title attribute
        var html = "<a href=\"https://example.com\" title=\"Example Site\">Example</a>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("[Example](https://example.com"), "expected content to contain: [Example](https://example.com");
        assertTrue(content.contains("Example Site"), "expected content to contain: Example Site");
    }

    @Test
    void testOrderedList() {
        // Ordered list
        var html = "<ol><li>First</li><li>Second</li><li>Third</li></ol>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("1. First"), "expected content to contain: 1. First");
        assertTrue(content.contains("2. Second"), "expected content to contain: 2. Second");
        assertTrue(content.contains("3. Third"), "expected content to contain: 3. Third");
    }

    @Test
    void testSimpleTable() {
        // Simple table with header
        var html = "<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("Name"), "expected content to contain: Name");
        assertTrue(content.contains("Age"), "expected content to contain: Age");
        assertTrue(content.contains("Alice"), "expected content to contain: Alice");
        assertTrue(content.contains("30"), "expected content to contain: 30");
        assertTrue(content.contains("|"), "expected content to contain: |");
        assertTrue(content.contains("---"), "expected content to contain: ---");
    }

    @Test
    void testUnorderedList() {
        // Unordered list
        var html = "<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>";
        var result = HtmlToMarkdown.convert(html);
        var content = result.content() != null ? result.content() : "";

        assertTrue(content.contains("- Item 1"), "expected content to contain: - Item 1");
        assertTrue(content.contains("- Item 2"), "expected content to contain: - Item 2");
        assertTrue(content.contains("- Item 3"), "expected content to contain: - Item 3");
    }

}

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// E2e tests for category: conversion

test "blockquote_multiple_paragraphs" {
    // Blockquote with multiple paragraphs has each paragraph prefixed
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>");
    try testing.expect(std.mem.indexOf(u8, result.content, "> First paragraph.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "> Second paragraph.") != null);
}

test "blockquote_nested" {
    // Nested blockquote produces double-prefixed lines
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Outer quote.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Inner quote.") != null);
}

test "blockquote_simple" {
    // Simple blockquote
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<blockquote><p>Quote text</p></blockquote>");
    try testing.expect(std.mem.indexOf(u8, result.content, "> Quote text") != null);
}

test "blockquote_with_list" {
    // Blockquote containing a list preserves list items inside quote
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Quote intro:") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Point one") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Point two") != null);
}

test "bold_and_italic" {
    // Nested bold and italic
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><strong><em>both</em></strong></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "***both***") != null);
}

test "bold_strong" {
    // Strong tag converts to bold
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><strong>bold</strong></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "**bold**") != null);
}

test "code_block" {
    // Code block with language preserves content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<pre><code class=\"language-python\">print('hello')</code></pre>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "print('hello')") != null);
}

test "code_block_no_language" {
    // Code block without a language class preserves content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<pre><code>plain code here</code></pre>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "plain code here") != null);
}

test "code_inline_in_paragraph" {
    // Inline code element nested inside a paragraph
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Call the <code>initialize()</code> method first.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "`initialize()`") != null);
}

test "code_with_backticks_in_content" {
    // Inline code containing backtick characters is properly escaped
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Use <code>`backtick` here</code> carefully.</p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "backtick") != null);
}

test "emphasis_mark_highlight" {
    // mark tag produces highlighted output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><mark>highlighted</mark></p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "highlighted") != null);
}

test "emphasis_strikethrough_del" {
    // del tag converts to GFM strikethrough
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><del>deleted text</del></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "~~deleted text~~") != null);
}

test "emphasis_strikethrough_s" {
    // s tag converts to GFM strikethrough
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><s>strikethrough</s></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "~~strikethrough~~") != null);
}

test "emphasis_subscript" {
    // sub tag content is preserved
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>H<sub>2</sub>O</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "H") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "O") != null);
}

test "emphasis_superscript" {
    // sup tag content is preserved
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>x<sup>2</sup></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "x") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "2") != null);
}

test "emphasis_underline_u" {
    // u tag content is preserved in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><u>underlined</u></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "underlined") != null);
}

test "form_input_elements" {
    // Form input elements produce readable output without form mechanics
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Name") != null);
}

test "form_select_options" {
    // Select element with options produces readable output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Color") != null);
}

test "form_textarea" {
    // Textarea element produces readable output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<form><label>Message:</label><textarea>Default text content</textarea></form>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Message") != null);
}

test "heading_h1" {
    // H1 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h1>Heading 1</h1>");
    try testing.expectEqual("# Heading 1", result.content);
}

test "heading_h2" {
    // H2 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h2>Heading 2</h2>");
    try testing.expectEqual("## Heading 2", result.content);
}

test "heading_h3" {
    // H3 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h3>Heading 3</h3>");
    try testing.expectEqual("### Heading 3", result.content);
}

test "heading_h4" {
    // H4 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h4>Heading 4</h4>");
    try testing.expectEqual("#### Heading 4", result.content);
}

test "heading_h5" {
    // H5 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h5>Heading 5</h5>");
    try testing.expectEqual("##### Heading 5", result.content);
}

test "heading_h6" {
    // H6 heading
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<h6>Heading 6</h6>");
    try testing.expectEqual("###### Heading 6", result.content);
}

test "image_figure_figcaption" {
    // Figure with figcaption preserves both image and caption
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>");
    try testing.expect(std.mem.indexOf(u8, result.content, "![A sunset](sunset.jpg)") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Beautiful sunset over the ocean") != null);
}

test "image_linked" {
    // Image inside an anchor produces a linked image
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "![Icon](icon.png)") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "https://example.com") != null);
}

test "image_no_alt" {
    // Image without alt text produces image markdown
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<img src=\"banner.jpg\">");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "banner.jpg") != null);
}

test "image_simple" {
    // Image with alt text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<img src=\"photo.jpg\" alt=\"A photo\">");
    try testing.expect(std.mem.indexOf(u8, result.content, "![A photo](photo.jpg)") != null);
}

test "image_with_title" {
    // Image with title attribute includes title in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">");
    try testing.expect(std.mem.indexOf(u8, result.content, "![Sales chart](chart.png") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Q3 Sales") != null);
}

test "inline_code" {
    // Inline code
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Use <code>console.log()</code> to debug</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "`console.log()`") != null);
}

test "italic_em" {
    // Em tag converts to italic
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p><em>italic</em></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "*italic*") != null);
}

test "line_break_br_tag" {
    // Single br tag produces a line break in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>First line.<br>Second line.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "First line.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Second line.") != null);
}

test "line_break_hr_tag" {
    // hr tag produces a horizontal separator between content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Before rule.</p><hr><p>After rule.</p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Before rule.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "After rule.") != null);
}

test "line_break_multiple_br" {
    // Multiple consecutive br tags in sequence
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Start.<br><br>End.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Start.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "End.") != null);
}

test "link_anchor_fragment" {
    // Fragment-only anchor link is preserved
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"#section\">Jump to section</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[Jump to section](#section)") != null);
}

test "link_empty_href" {
    // Link with empty href produces output with the link text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"\">No destination</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "No destination") != null);
}

test "link_image_inside" {
    // Image inside a link produces a linked image
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "![Logo](logo.png)") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "https://example.com") != null);
}

test "link_mailto" {
    // Mailto link is preserved with mailto: scheme
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"mailto:user@example.com\">Email us</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "mailto:user@example.com") != null);
}

test "link_simple" {
    // Simple link
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\">Example</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[Example](https://example.com)") != null);
}

test "link_with_bold_text" {
    // Link containing bold text preserves formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\"><strong>Bold link</strong></a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "**Bold link**") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "https://example.com") != null);
}

test "link_with_title" {
    // Link with title attribute
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<a href=\"https://example.com\" title=\"Example Site\">Example</a>");
    try testing.expect(std.mem.indexOf(u8, result.content, "[Example](https://example.com") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Example Site") != null);
}

test "list_definition_dl" {
    // Definition list with dt and dd elements
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Term One") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Definition of term one.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Term Two") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Definition of term two.") != null);
}

test "list_item_multiple_paragraphs" {
    // List item containing multiple paragraphs
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>");
    try testing.expect(std.mem.indexOf(u8, result.content, "First paragraph in item.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Second paragraph in item.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Simple item") != null);
}

test "list_mixed_nested" {
    // Mixed list: ordered list nested inside unordered list
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Item A") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Sub 1") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Sub 2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Item B") != null);
}

test "list_nested_ordered" {
    // Nested ordered list with two levels of depth
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Step 1") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Step 1a") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Step 1b") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Step 2") != null);
}

test "list_nested_unordered" {
    // Nested unordered list with two levels of depth
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Parent A") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Child A1") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Child A2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Parent B") != null);
}

test "list_task_checkboxes" {
    // Task list with checked and unchecked checkboxes
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Done task") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Pending task") != null);
}

test "ordered_list" {
    // Ordered list
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ol><li>First</li><li>Second</li><li>Third</li></ol>");
    try testing.expect(std.mem.indexOf(u8, result.content, "1. First") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "2. Second") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "3. Third") != null);
}

test "paragraph_multiple" {
    // Multiple paragraphs are separated by a blank line
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>First paragraph.</p><p>Second paragraph.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "First paragraph.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Second paragraph.") != null);
}

test "paragraph_nested_divs" {
    // Text nested inside divs is extracted correctly
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<div><div><p>Nested text</p></div></div>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Nested text") != null);
}

test "paragraph_simple" {
    // Simple paragraph converts to plain text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Hello World</p>");
    try testing.expectEqual("Hello World", result.content);
}

test "paragraph_with_inline_formatting" {
    // Paragraph with bold, italic, and a link
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "**bold**") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "*italic*") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "[link](https://example.com)") != null);
}

test "paragraph_with_line_breaks" {
    // Paragraph with br tags produces line breaks in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Line one.<br>Line two.<br>Line three.</p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Line one.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Line two.") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Line three.") != null);
}

test "semantic_abbr" {
    // Abbreviation element text is preserved
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "WWW") != null);
}

test "semantic_article" {
    // Article element wrapping content preserves inner content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<article><h2>Article Title</h2><p>Article body.</p></article>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Article Title") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Article body.") != null);
}

test "semantic_definition_list" {
    // Definition list with term and description
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>");
    try testing.expect(std.mem.indexOf(u8, result.content, "HTML") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "HyperText Markup Language") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "CSS") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Cascading Style Sheets") != null);
}

test "semantic_details_summary" {
    // Details and summary elements produce readable output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<details><summary>Click to expand</summary><p>Hidden content here.</p></details>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Click to expand") != null);
}

test "semantic_hr" {
    // Horizontal rule produces a separator in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>Above</p><hr><p>Below</p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Above") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Below") != null);
}

test "semantic_mark_highlight" {
    // Mark tag produces highlighted output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>This is <mark>highlighted text</mark> in a sentence.</p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "highlighted text") != null);
}

test "semantic_section_with_heading" {
    // Section element with heading preserves structure
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<section><h3>Section Heading</h3><p>Section content.</p></section>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Section Heading") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Section content.") != null);
}

test "semantic_sub_superscript" {
    // Subscript and superscript elements are preserved in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "H") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "O") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "E=mc") != null);
}

test "simple_table" {
    // Simple table with header
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Name") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Age") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Alice") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "30") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "|") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "---") != null);
}

test "table_empty" {
    // Empty table produces no output or minimal output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table></table>");
    try testing.expectEqual("", result.content);
}

test "table_no_thead" {
    // Table without thead uses first row as implied header
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Product") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Price") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Apple") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "1.00") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "|") != null);
}

test "table_pipe_chars_in_content" {
    // Table cells containing pipe characters are escaped in output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Expression") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Result") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "true") != null);
}

test "table_with_alignment" {
    // Table with column alignment attributes
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Left") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Center") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Right") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "L") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "C") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "R") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "|") != null);
}

test "table_with_colspan" {
    // Table with colspan attribute in a header cell
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>");
    try testing.expect(result.content.len > 0);
    try testing.expect(std.mem.indexOf(u8, result.content, "Full Name") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "John") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Doe") != null);
}

test "unordered_list" {
    // Unordered list
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = html_to_markdown_rs.convert("<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>");
    try testing.expect(std.mem.indexOf(u8, result.content, "- Item 1") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "- Item 2") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "- Item 3") != null);
}

#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: conversion
set -euo pipefail

test_blockquote_multiple_paragraphs() {
  # Blockquote with multiple paragraphs has each paragraph prefixed
  local output
  output=$(html_to_markdown convert)

}

test_blockquote_nested() {
  # Nested blockquote produces double-prefixed lines
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_blockquote_simple() {
  # Simple blockquote
  local output
  output=$(html_to_markdown convert)

}

test_blockquote_with_list() {
  # Blockquote containing a list preserves list items inside quote
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_bold_and_italic() {
  # Nested bold and italic
  local output
  output=$(html_to_markdown convert)

}

test_bold_strong() {
  # Strong tag converts to bold
  local output
  output=$(html_to_markdown convert)

}

test_code_block() {
  # Code block with language preserves content
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_code_block_no_language() {
  # Code block without a language class preserves content
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_code_inline_in_paragraph() {
  # Inline code element nested inside a paragraph
  local output
  output=$(html_to_markdown convert)

}

test_code_with_backticks_in_content() {
  # Inline code containing backtick characters is properly escaped
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_emphasis_mark_highlight() {
  # mark tag produces highlighted output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_emphasis_strikethrough_del() {
  # del tag converts to GFM strikethrough
  local output
  output=$(html_to_markdown convert)

}

test_emphasis_strikethrough_s() {
  # s tag converts to GFM strikethrough
  local output
  output=$(html_to_markdown convert)

}

test_emphasis_subscript() {
  # sub tag content is preserved
  local output
  output=$(html_to_markdown convert)

}

test_emphasis_superscript() {
  # sup tag content is preserved
  local output
  output=$(html_to_markdown convert)

}

test_emphasis_underline_u() {
  # u tag content is preserved in output
  local output
  output=$(html_to_markdown convert)

}

test_form_input_elements() {
  # Form input elements produce readable output without form mechanics
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_form_select_options() {
  # Select element with options produces readable output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_form_textarea() {
  # Textarea element produces readable output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_heading_h1() {
  # H1 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '# Heading 1' 'content'
}

test_heading_h2() {
  # H2 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '## Heading 2' 'content'
}

test_heading_h3() {
  # H3 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '### Heading 3' 'content'
}

test_heading_h4() {
  # H4 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '#### Heading 4' 'content'
}

test_heading_h5() {
  # H5 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '##### Heading 5' 'content'
}

test_heading_h6() {
  # H6 heading
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '###### Heading 6' 'content'
}

test_image_figure_figcaption() {
  # Figure with figcaption preserves both image and caption
  local output
  output=$(html_to_markdown convert)

}

test_image_linked() {
  # Image inside an anchor produces a linked image
  local output
  output=$(html_to_markdown convert)

}

test_image_no_alt() {
  # Image without alt text produces image markdown
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_image_simple() {
  # Image with alt text
  local output
  output=$(html_to_markdown convert)

}

test_image_with_title() {
  # Image with title attribute includes title in output
  local output
  output=$(html_to_markdown convert)

}

test_inline_code() {
  # Inline code
  local output
  output=$(html_to_markdown convert)

}

test_italic_em() {
  # Em tag converts to italic
  local output
  output=$(html_to_markdown convert)

}

test_line_break_br_tag() {
  # Single br tag produces a line break in output
  local output
  output=$(html_to_markdown convert)

}

test_line_break_hr_tag() {
  # hr tag produces a horizontal separator between content
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_line_break_multiple_br() {
  # Multiple consecutive br tags in sequence
  local output
  output=$(html_to_markdown convert)

}

test_link_anchor_fragment() {
  # Fragment-only anchor link is preserved
  local output
  output=$(html_to_markdown convert)

}

test_link_empty_href() {
  # Link with empty href produces output with the link text
  local output
  output=$(html_to_markdown convert)

}

test_link_image_inside() {
  # Image inside a link produces a linked image
  local output
  output=$(html_to_markdown convert)

}

test_link_mailto() {
  # Mailto link is preserved with mailto: scheme
  local output
  output=$(html_to_markdown convert)

}

test_link_simple() {
  # Simple link
  local output
  output=$(html_to_markdown convert)

}

test_link_with_bold_text() {
  # Link containing bold text preserves formatting
  local output
  output=$(html_to_markdown convert)

}

test_link_with_title() {
  # Link with title attribute
  local output
  output=$(html_to_markdown convert)

}

test_list_definition_dl() {
  # Definition list with dt and dd elements
  local output
  output=$(html_to_markdown convert)

}

test_list_item_multiple_paragraphs() {
  # List item containing multiple paragraphs
  local output
  output=$(html_to_markdown convert)

}

test_list_mixed_nested() {
  # Mixed list: ordered list nested inside unordered list
  local output
  output=$(html_to_markdown convert)

}

test_list_nested_ordered() {
  # Nested ordered list with two levels of depth
  local output
  output=$(html_to_markdown convert)

}

test_list_nested_unordered() {
  # Nested unordered list with two levels of depth
  local output
  output=$(html_to_markdown convert)

}

test_list_task_checkboxes() {
  # Task list with checked and unchecked checkboxes
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_ordered_list() {
  # Ordered list
  local output
  output=$(html_to_markdown convert)

}

test_paragraph_multiple() {
  # Multiple paragraphs are separated by a blank line
  local output
  output=$(html_to_markdown convert)

}

test_paragraph_nested_divs() {
  # Text nested inside divs is extracted correctly
  local output
  output=$(html_to_markdown convert)

}

test_paragraph_simple() {
  # Simple paragraph converts to plain text
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" 'Hello World' 'content'
}

test_paragraph_with_inline_formatting() {
  # Paragraph with bold, italic, and a link
  local output
  output=$(html_to_markdown convert)

}

test_paragraph_with_line_breaks() {
  # Paragraph with br tags produces line breaks in output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_semantic_abbr() {
  # Abbreviation element text is preserved
  local output
  output=$(html_to_markdown convert)

}

test_semantic_article() {
  # Article element wrapping content preserves inner content
  local output
  output=$(html_to_markdown convert)

}

test_semantic_definition_list() {
  # Definition list with term and description
  local output
  output=$(html_to_markdown convert)

}

test_semantic_details_summary() {
  # Details and summary elements produce readable output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_semantic_hr() {
  # Horizontal rule produces a separator in output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_semantic_mark_highlight() {
  # Mark tag produces highlighted output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_semantic_section_with_heading() {
  # Section element with heading preserves structure
  local output
  output=$(html_to_markdown convert)

}

test_semantic_sub_superscript() {
  # Subscript and superscript elements are preserved in output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_simple_table() {
  # Simple table with header
  local output
  output=$(html_to_markdown convert)

}

test_table_empty() {
  # Empty table produces no output or minimal output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '' 'content'
}

test_table_no_thead() {
  # Table without thead uses first row as implied header
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_table_pipe_chars_in_content() {
  # Table cells containing pipe characters are escaped in output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_table_with_alignment() {
  # Table with column alignment attributes
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_table_with_colspan() {
  # Table with colspan attribute in a header cell
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

test_unordered_list() {
  # Unordered list
  local output
  output=$(html_to_markdown convert)

}

run_tests_conversion() {
  run_test test_blockquote_multiple_paragraphs
  run_test test_blockquote_nested
  run_test test_blockquote_simple
  run_test test_blockquote_with_list
  run_test test_bold_and_italic
  run_test test_bold_strong
  run_test test_code_block
  run_test test_code_block_no_language
  run_test test_code_inline_in_paragraph
  run_test test_code_with_backticks_in_content
  run_test test_emphasis_mark_highlight
  run_test test_emphasis_strikethrough_del
  run_test test_emphasis_strikethrough_s
  run_test test_emphasis_subscript
  run_test test_emphasis_superscript
  run_test test_emphasis_underline_u
  run_test test_form_input_elements
  run_test test_form_select_options
  run_test test_form_textarea
  run_test test_heading_h1
  run_test test_heading_h2
  run_test test_heading_h3
  run_test test_heading_h4
  run_test test_heading_h5
  run_test test_heading_h6
  run_test test_image_figure_figcaption
  run_test test_image_linked
  run_test test_image_no_alt
  run_test test_image_simple
  run_test test_image_with_title
  run_test test_inline_code
  run_test test_italic_em
  run_test test_line_break_br_tag
  run_test test_line_break_hr_tag
  run_test test_line_break_multiple_br
  run_test test_link_anchor_fragment
  run_test test_link_empty_href
  run_test test_link_image_inside
  run_test test_link_mailto
  run_test test_link_simple
  run_test test_link_with_bold_text
  run_test test_link_with_title
  run_test test_list_definition_dl
  run_test test_list_item_multiple_paragraphs
  run_test test_list_mixed_nested
  run_test test_list_nested_ordered
  run_test test_list_nested_unordered
  run_test test_list_task_checkboxes
  run_test test_ordered_list
  run_test test_paragraph_multiple
  run_test test_paragraph_nested_divs
  run_test test_paragraph_simple
  run_test test_paragraph_with_inline_formatting
  run_test test_paragraph_with_line_breaks
  run_test test_semantic_abbr
  run_test test_semantic_article
  run_test test_semantic_definition_list
  run_test test_semantic_details_summary
  run_test test_semantic_hr
  run_test test_semantic_mark_highlight
  run_test test_semantic_section_with_heading
  run_test test_semantic_sub_superscript
  run_test test_simple_table
  run_test test_table_empty
  run_test test_table_no_thead
  run_test test_table_pipe_chars_in_content
  run_test test_table_with_alignment
  run_test test_table_with_colspan
  run_test test_unordered_list
}

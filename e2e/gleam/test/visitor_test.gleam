// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import html_to_markdown

import gleam/string

// Visitor replaces audio element with custom output
pub fn visitor_audio_custom_test() {
  let result = html_to_markdown.convert("<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>")
  result |> should.be_ok()
  result.content |> string.contains("[AUDIO: podcast.mp3]") |> should.equal(True)
  result.content |> string.contains("Listen to this:") |> should.equal(True)
}

// Visitor removes audio elements from output
pub fn visitor_audio_skip_test() {
  let result = html_to_markdown.convert("<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>")
  result |> should.be_ok()
  result.content |> string.contains("Background music:") |> should.equal(True)
  result.content |> string.contains("Enjoy!") |> should.equal(True)
  result.content |> string.contains("music.ogg") |> should.equal(False)
}

// Visitor replaces button with bracketed text
pub fn visitor_button_custom_test() {
  let result = html_to_markdown.convert("<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>")
  result |> should.be_ok()
  result.content |> string.contains("[BTN:Click me]") |> should.equal(True)
  result.content |> string.contains("[BTN:Cancel]") |> should.equal(True)
  result.content |> string.contains("Confirm action:") |> should.equal(True)
}

// Visitor removes all buttons from output
pub fn visitor_button_skip_test() {
  let result = html_to_markdown.convert("<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>")
  result |> should.be_ok()
  result.content |> string.contains("Actions available:") |> should.equal(True)
  result.content |> string.contains("Save") |> should.equal(False)
  result.content |> string.contains("Delete") |> should.equal(False)
  result.content |> string.contains("Export") |> should.equal(False)
}

// Visitor continue action preserves default conversion
pub fn visitor_continue_default_test() {
  let result = html_to_markdown.convert("<p>Hello <strong>World</strong></p>")
  result |> should.be_ok()
  result.content |> string.contains("**World**") |> should.equal(True)
}

// Visitor replaces blockquote with custom format
pub fn visitor_custom_blockquote_test() {
  let result = html_to_markdown.convert("<blockquote><p>A wise quote.</p></blockquote>")
  result |> should.be_ok()
  result.content |> string.contains("QUOTE:") |> should.equal(True)
  result.content |> string.contains("A wise quote.") |> should.equal(True)
}

// Visitor replaces emphasis with custom output
pub fn visitor_custom_emphasis_test() {
  let result = html_to_markdown.convert("<p>This is <em>important</em> text.</p>")
  result |> should.be_ok()
  result.content |> string.contains(">>>important<<<") |> should.equal(True)
  result.content |> string.contains("*important*") |> should.equal(False)
}

// Visitor replaces heading with custom format
pub fn visitor_custom_heading_test() {
  let result = html_to_markdown.convert("<h2>Section Title</h2><p>Content below heading.</p>")
  result |> should.be_ok()
  result.content |> string.contains("--- Section Title ---") |> should.equal(True)
  result.content |> string.contains("## Section Title") |> should.equal(False)
  result.content |> string.contains("Content below heading.") |> should.equal(True)
}

// Visitor replaces image with custom output using template
pub fn visitor_custom_image_test() {
  let result = html_to_markdown.convert("<img src=\"banner.png\" alt=\"Banner\">")
  result |> should.be_ok()
  result.content |> string.contains("[Image: Banner]") |> should.equal(True)
  result.content |> string.contains("banner.png") |> should.equal(False)
}

// Visitor reformats links using template interpolation
pub fn visitor_custom_link_format_test() {
  let result = html_to_markdown.convert("<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Example (https://example.com)") |> should.equal(True)
  result.content |> string.contains("[Example]") |> should.equal(False)
}

// Visitor replaces link with static custom output
pub fn visitor_custom_link_static_test() {
  let result = html_to_markdown.convert("<a href=\"https://example.com\">Click here</a>")
  result |> should.be_ok()
  result.content |> string.contains("[REDACTED LINK]") |> should.equal(True)
  result.content |> string.contains("example.com") |> should.equal(False)
}

// Visitor custom action replaces element output
pub fn visitor_custom_output_test() {
  let result = html_to_markdown.convert("<h1>Original Heading</h1>")
  result |> should.be_ok()
  result.content |> string.contains("## REPLACED HEADING") |> should.equal(True)
  result.content |> string.contains("# Original Heading") |> should.equal(False)
}

// Visitor customizes definition list items
pub fn visitor_definition_list_custom_test() {
  let result = html_to_markdown.convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>")
  result |> should.be_ok()
  result.content |> string.contains("**HTML**") |> should.equal(True)
  result.content |> string.contains("**CSS**") |> should.equal(True)
  result.content |> string.contains("HyperText Markup Language") |> should.equal(True)
}

// Visitor formats definition lists with custom templates
pub fn visitor_definition_list_custom_format_test() {
  let result = html_to_markdown.convert("<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>")
  result |> should.be_ok()
  result.content |> string.contains("### Python") |> should.equal(True)
  result.content |> string.contains("### JavaScript") |> should.equal(True)
  result.content |> string.contains("> A high-level programming language") |> should.equal(True)
  result.content |> string.contains("> A scripting language for web browsers") |> should.equal(True)
}

// Visitor skips definition list items from output
pub fn visitor_definition_list_skip_test() {
  let result = html_to_markdown.convert("<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>")
  result |> should.be_ok()
  result.content |> string.contains("Glossary:") |> should.equal(True)
  result.content |> string.contains("End of glossary") |> should.equal(True)
  result.content |> string.contains("Term A") |> should.equal(False)
  result.content |> string.contains("Definition") |> should.equal(False)
}

// Visitor customizes details/summary disclosure elements
pub fn visitor_details_summary_custom_test() {
  let result = html_to_markdown.convert("<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>")
  result |> should.be_ok()
  result.content |> string.contains("[EXPANDABLE] Click to expand") |> should.equal(True)
  result.content |> string.contains("This content is initially hidden.") |> should.equal(True)
}

// Visitor removes details/summary elements entirely
pub fn visitor_details_summary_skip_test() {
  let result = html_to_markdown.convert("<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Main content here.") |> should.equal(True)
  result.content |> string.contains("More main content.") |> should.equal(True)
  result.content |> string.contains("Hidden section") |> should.equal(False)
  result.content |> string.contains("Secret details") |> should.equal(False)
}

// Visitor customizes figure and figcaption elements
pub fn visitor_figure_custom_test() {
  let result = html_to_markdown.convert("<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>")
  result |> should.be_ok()
  result.content |> string.contains("Article Title") |> should.equal(True)
  result.content |> string.contains("*Figure 1: System Architecture*") |> should.equal(True)
  result.content |> string.contains("Explanation of the figure.") |> should.equal(True)
}

// Visitor wraps figure content with custom formatting
pub fn visitor_figure_custom_wrap_test() {
  let result = html_to_markdown.convert("<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>")
  result |> should.be_ok()
  result.content |> string.contains("[FIGURE]") |> should.equal(True)
  result.content |> string.contains("[/FIGURE]") |> should.equal(True)
  result.content |> string.contains("Gallery") |> should.equal(True)
}

// Visitor removes figure elements with their captions
pub fn visitor_figure_skip_test() {
  let result = html_to_markdown.convert("<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>")
  result |> should.be_ok()
  result.content |> string.contains("See the chart below:") |> should.equal(True)
  result.content |> string.contains("As shown in the chart above.") |> should.equal(True)
  result.content |> string.contains("Revenue Trends") |> should.equal(False)
  result.content |> string.contains("chart.svg") |> should.equal(False)
}

// Visitor replaces form with custom output
pub fn visitor_form_custom_test() {
  let result = html_to_markdown.convert("<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>")
  result |> should.be_ok()
  result.content |> string.contains("[FORM PLACEHOLDER]") |> should.equal(True)
  result.content |> string.contains("submit") |> should.equal(False)
  result.content |> string.contains("input") |> should.equal(False)
}

// Visitor skips form elements entirely
pub fn visitor_form_skip_test() {
  let result = html_to_markdown.convert("<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>")
  result |> should.be_ok()
  result.content |> string.contains("Before form") |> should.equal(True)
  result.content |> string.contains("After form") |> should.equal(True)
  result.content |> string.contains("email") |> should.equal(False)
}

// Visitor replaces horizontal rule with custom output
pub fn visitor_horizontal_rule_custom_test() {
  let result = html_to_markdown.convert("<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>")
  result |> should.be_ok()
  result.content |> string.contains("[DIVIDER]") |> should.equal(True)
  result.content |> string.contains("Section A") |> should.equal(True)
  result.content |> string.contains("Section B") |> should.equal(True)
  result.content |> string.contains("---") |> should.equal(False)
}

// Visitor removes all horizontal rules
pub fn visitor_horizontal_rule_skip_test() {
  let result = html_to_markdown.convert("<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>")
  result |> should.be_ok()
  result.content |> string.contains("Part 1") |> should.equal(True)
  result.content |> string.contains("Part 2") |> should.equal(True)
  result.content |> string.contains("Part 3") |> should.equal(True)
  result.content |> string.contains("---") |> should.equal(False)
}

// Visitor replaces embedded iframe with custom text
pub fn visitor_iframe_custom_test() {
  let result = html_to_markdown.convert("<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>")
  result |> should.be_ok()
  result.content |> string.contains("[EMBEDDED: https://maps.example.com/embed]") |> should.equal(True)
  result.content |> string.contains("Embedded map:") |> should.equal(True)
  result.content |> string.contains("End of map") |> should.equal(True)
}

// Visitor removes embedded iframes
pub fn visitor_iframe_skip_test() {
  let result = html_to_markdown.convert("<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Reviews") |> should.equal(True)
  result.content |> string.contains("See reviews from our partners.") |> should.equal(True)
  result.content |> string.contains("widget.example.com") |> should.equal(False)
}

// Visitor replaces input with labeled output
pub fn visitor_input_custom_test() {
  let result = html_to_markdown.convert("<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>")
  result |> should.be_ok()
  result.content |> string.contains("[INPUT:text]") |> should.equal(True)
  result.content |> string.contains("[INPUT:password]") |> should.equal(True)
}

// Visitor skips all input elements
pub fn visitor_input_skip_test() {
  let result = html_to_markdown.convert("<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>")
  result |> should.be_ok()
  result.content |> string.contains("Sign up:") |> should.equal(True)
  result.content |> string.contains("Continue") |> should.equal(True)
  result.content |> string.contains("email") |> should.equal(False)
}

// Visitor replaces line break with custom output
pub fn visitor_line_break_custom_test() {
  let result = html_to_markdown.convert("<p>First line<br>Second line<br>Third line</p>")
  result |> should.be_ok()
  result.content |> string.contains("First line | Second line | Third line") |> should.equal(True)
  result.content |> string.contains("\n\n") |> should.equal(False)
}

// Visitor removes all line breaks
pub fn visitor_line_break_skip_test() {
  let result = html_to_markdown.convert("<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>")
  result |> should.be_ok()
  result.content |> string.contains("Address Line 1Address Line 2Address Line 3") |> should.equal(True)
}

// Visitor replaces highlight/mark with custom template
pub fn visitor_mark_custom_test() {
  let result = html_to_markdown.convert("<p>This is a <mark>highlighted passage</mark> in the text.</p>")
  result |> should.be_ok()
  result.content |> string.contains("==highlighted passage==") |> should.equal(True)
  result.content |> string.contains("This is a") |> should.equal(True)
}

// Visitor skips mark elements entirely
pub fn visitor_mark_skip_test() {
  let result = html_to_markdown.convert("<p>Key insight: <mark>always validate input</mark> for security.</p>")
  result |> should.be_ok()
  result.content |> string.contains("always validate input") |> should.equal(False)
  result.content |> string.contains("Key insight:") |> should.equal(True)
  result.content |> string.contains("for security.") |> should.equal(True)
}

// Visitor preserve_html action includes raw HTML in output
pub fn visitor_preserve_html_test() {
  let result = html_to_markdown.convert("<div><custom-tag>Custom content</custom-tag></div>")
  result |> should.be_ok()
  result.content |> string.contains("<custom-tag>") |> should.equal(True)
}

// Visitor skips all headings from document
pub fn visitor_skip_all_headings_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><p>Body text remains.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Title") |> should.equal(False)
  result.content |> string.contains("Body text remains.") |> should.equal(True)
}

// Visitor skips code blocks from output
pub fn visitor_skip_code_blocks_test() {
  let result = html_to_markdown.convert("<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>")
  result |> should.be_ok()
  result.content |> string.contains("Intro text") |> should.equal(True)
  result.content |> string.contains("Outro text") |> should.equal(True)
  result.content |> string.contains("let x = 42") |> should.equal(False)
}

// Visitor skip action omits all headings from output
pub fn visitor_skip_heading_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><p>Body text remains.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Title") |> should.equal(False)
  result.content |> string.contains("Body text remains.") |> should.equal(True)
}

// Visitor skips all images from output
pub fn visitor_skip_images_test() {
  let result = html_to_markdown.convert("<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>")
  result |> should.be_ok()
  result.content |> string.contains("Before image") |> should.equal(True)
  result.content |> string.contains("After image") |> should.equal(True)
  result.content |> string.contains("photo.jpg") |> should.equal(False)
  result.content |> string.contains("A photo") |> should.equal(False)
}

// Visitor skips all links entirely
pub fn visitor_skip_links_test() {
  let result = html_to_markdown.convert("<p>Before <a href=\"https://example.com\">link text</a> after</p>")
  result |> should.be_ok()
  result.content |> string.contains("link text") |> should.equal(False)
  result.content |> string.contains("example.com") |> should.equal(False)
}

// Visitor skips bold/strong elements
pub fn visitor_skip_strong_test() {
  let result = html_to_markdown.convert("<p>Normal <strong>bold text</strong> normal</p>")
  result |> should.be_ok()
  result.content |> string.contains("bold text") |> should.equal(False)
  result.content |> string.contains("Normal") |> should.equal(True)
}

// Visitor replaces subscript with custom template
pub fn visitor_subscript_custom_test() {
  let result = html_to_markdown.convert("<p>H<sub>2</sub>O is water.</p>")
  result |> should.be_ok()
  result.content |> string.contains("H~2~O") |> should.equal(True)
  result.content |> string.contains("is water") |> should.equal(True)
}

// Visitor skips subscript elements entirely
pub fn visitor_subscript_skip_test() {
  let result = html_to_markdown.convert("<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>")
  result |> should.be_ok()
  result.content |> string.contains("The formula CHO is sugar.") |> should.equal(True)
}

// Visitor replaces superscript with custom template
pub fn visitor_superscript_custom_test() {
  let result = html_to_markdown.convert("<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>")
  result |> should.be_ok()
  result.content |> string.contains("E=mc^2^") |> should.equal(True)
  result.content |> string.contains("revolutionized physics") |> should.equal(True)
}

// Visitor skips superscript from output
pub fn visitor_superscript_skip_test() {
  let result = html_to_markdown.convert("<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>")
  result |> should.be_ok()
  result.content |> string.contains("The equation x + y = z has no solutions.") |> should.equal(True)
}

// Visitor replaces underline with custom markup
pub fn visitor_underline_custom_test() {
  let result = html_to_markdown.convert("<p>This is <u>very important</u> text.</p>")
  result |> should.be_ok()
  result.content |> string.contains("_very important_") |> should.equal(True)
  result.content |> string.contains("**") |> should.equal(False)
}

// Visitor skips underline elements from output
pub fn visitor_underline_skip_test() {
  let result = html_to_markdown.convert("<p>Normal text with <u>underlined part</u> and more text.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Normal text with") |> should.equal(True)
  result.content |> string.contains("and more text.") |> should.equal(True)
  result.content |> string.contains("underlined part") |> should.equal(False)
}

// Visitor replaces video with custom link
pub fn visitor_video_custom_test() {
  let result = html_to_markdown.convert("<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>")
  result |> should.be_ok()
  result.content |> string.contains("[VIDEO: tutorial.mp4]") |> should.equal(True)
  result.content |> string.contains("Watch our tutorial:") |> should.equal(True)
  result.content |> string.contains("Great content!") |> should.equal(True)
}

// Visitor removes video elements entirely
pub fn visitor_video_skip_test() {
  let result = html_to_markdown.convert("<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>")
  result |> should.be_ok()
  result.content |> string.contains("Demo") |> should.equal(True)
  result.content |> string.contains("See the demo above.") |> should.equal(True)
  result.content |> string.contains("demo.webm") |> should.equal(False)
}

#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: smoke
set -euo pipefail

test_smoke_empty_string() {
  # Empty string produces empty output
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" '' 'content'
}

test_smoke_simple_heading() {
  # H1 heading converts to ATX markdown
  local output
  output=$(html_to_markdown convert)

}

test_smoke_simple_paragraph() {
  # Simple paragraph converts correctly
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_equals "$val_content" 'Hello World' 'content'
  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
}

run_tests_smoke() {
  run_test test_smoke_empty_string
  run_test test_smoke_simple_heading
  run_test test_smoke_simple_paragraph
}

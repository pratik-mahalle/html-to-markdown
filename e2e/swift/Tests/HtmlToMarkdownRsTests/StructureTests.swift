// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import HtmlToMarkdownRs

/// E2e tests for category: structure.
final class StructureTests: XCTestCase {
    func testStructureCodeBlock() throws {
        // Fenced code block produces Code node
        let result = try convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 2)
    }

    func testStructureDeepNestingH1H2H3() throws {
        // H1 > H2 > H3 creates three levels of heading nesting
        let result = try convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 5)
    }

    func testStructureH1H2NestedGroup() throws {
        // H1 followed by H2 creates a nested group under the H1
        let result = try convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 3)
    }

    func testStructureHeadingParagraph() throws {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        let result = try convert("<h1>Title</h1><p>A paragraph of text.</p>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 2)
    }

    func testStructureList() throws {
        // Unordered list produces List and ListItem nodes
        let result = try convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 2)
    }

    func testStructureMultipleHeadings() throws {
        // Multiple headings create multiple Heading nodes with correct levels
        let result = try convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 4)
    }

    func testStructureSiblingH1Groups() throws {
        // H1, H2, then another H1 creates two sibling top-level groups
        let result = try convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", "{\"includeDocumentStructure\":true}")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertFalse(result.document.nodes.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document.nodes.count, 4)
    }

}

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import html_to_markdown

import gleam/list
import gleam/string

// Bare URL links rendered as regular markdown links when autolinks disabled
pub fn options_autolinks_false_test() {
  let result = html_to_markdown.convert("<p><a href='https://example.com'>https://example.com</a></p>", "{\"autolinks\":false}")
  result |> should.be_ok()
  result.content |> string.contains("example.com") |> should.equal(True)
}

// BR elements in table cells are stripped when disabled
pub fn options_br_in_tables_false_test() {
  let result = html_to_markdown.convert("<table><tr><th>Col</th></tr><tr><td>A<br>B</td></tr></table>", "{\"brInTables\":false}")
  result |> should.be_ok()
  result.content |> string.contains("Col") |> should.equal(True)
}

// BR elements in table cells render as line breaks
pub fn options_br_in_tables_true_test() {
  let result = html_to_markdown.convert("<table><tr><th>Header</th></tr><tr><td>Line 1<br>Line 2</td></tr></table>", "{\"brInTables\":true}")
  result |> should.be_ok()
  result.content |> string.contains("Header") |> should.equal(True)
  result.content |> string.contains("Line 1") |> should.equal(True)
  result.content |> string.contains("Line 2") |> should.equal(True)
}

// Backticks code block style uses triple backtick fences
pub fn options_code_block_backticks_test() {
  let result = html_to_markdown.convert("<pre><code class=\"language-js\">console.log('hi');</code></pre>", "{\"codeBlockStyle\":\"Backticks\"}")
  result |> should.be_ok()
  result.content |> string.contains("```") |> should.equal(True)
  result.content |> string.contains("console.log('hi');") |> should.equal(True)
}

// Code blocks use 4-space indentation
pub fn options_code_block_indented_test() {
  let result = html_to_markdown.convert("<pre><code>print('hello')</code></pre>", "{\"codeBlockStyle\":\"Indented\"}")
  result |> should.be_ok()
  result.content |> string.contains("print('hello')") |> should.equal(True)
  result.content |> string.contains("```") |> should.equal(False)
}

// Code blocks use tilde fences
pub fn options_code_block_tildes_test() {
  let result = html_to_markdown.convert("<pre><code>let x = 1;</code></pre>", "{\"codeBlockStyle\":\"Tildes\"}")
  result |> should.be_ok()
  result.content |> string.contains("~~~") |> should.equal(True)
  result.content |> string.contains("let x = 1;") |> should.equal(True)
}

// Tildes code block style uses triple tilde fences
pub fn options_code_block_tildes_style_test() {
  let result = html_to_markdown.convert("<pre><code>some code</code></pre>", "{\"codeBlockStyle\":\"Tildes\"}")
  result |> should.be_ok()
  result.content |> string.contains("~~~") |> should.equal(True)
  result.content |> string.contains("some code") |> should.equal(True)
}

// Default code language annotation on blocks without lang attribute
pub fn options_code_language_python_test() {
  let result = html_to_markdown.convert("<pre><code>def hello(): pass</code></pre>", "{\"codeLanguage\":\"python\"}")
  result |> should.be_ok()
  result.content |> string.contains("```python") |> should.equal(True)
  result.content |> string.contains("def hello") |> should.equal(True)
}

// Block elements treated as inline
pub fn options_convert_as_inline_test() {
  let result = html_to_markdown.convert("<p>One</p><p>Two</p>", "{\"convertAsInline\":true}")
  result |> should.be_ok()
  result.content |> string.contains("One") |> should.equal(True)
  result.content |> string.contains("Two") |> should.equal(True)
}

// Debug mode enabled does not crash and produces output
pub fn options_debug_true_test() {
  let result = html_to_markdown.convert("<p>Debug test</p>", "{\"debug\":true}")
  result |> should.be_ok()
  result.content |> string.contains("Debug test") |> should.equal(True)
}

// Links without title get empty title attribute when defaultTitle is true
pub fn options_default_title_true_test() {
  let result = html_to_markdown.convert("<p><a href='https://example.com'>Link</a></p>", "{\"defaultTitle\":true}")
  result |> should.be_ok()
  result.content |> string.contains("Link") |> should.equal(True)
  result.content |> string.contains("https://example.com") |> should.equal(True)
}

// UTF-8 encoding hint for special characters
pub fn options_encoding_utf8_test() {
  let result = html_to_markdown.convert("<p>Café naïve résumé</p>", "{\"encoding\":\"utf-8\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
}

// ASCII Markdown characters are escaped when escapeAscii is true
pub fn options_escape_ascii_enabled_test() {
  let result = html_to_markdown.convert("<p>Text with # hash and [brackets] and * star</p>", "{\"escapeAscii\":true}")
  result |> should.be_ok()
  result.content |> string.contains("Text") |> should.equal(True)
  result.content |> string.contains("hash") |> should.equal(True)
  result.content |> string.contains("brackets") |> should.equal(True)
  result.content |> string.contains("star") |> should.equal(True)
}

// escape_asterisks option escapes asterisks in plain text
pub fn options_escape_asterisks_test() {
  let result = html_to_markdown.convert("<p>Use 2*3 = 6 in math.</p>", "{\"escapeAsterisks\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("2") |> should.equal(True)
  result.content |> string.contains("3") |> should.equal(True)
  result.content |> string.contains("6") |> should.equal(True)
}

// escape_misc option escapes miscellaneous markdown characters
pub fn options_escape_misc_test() {
  let result = html_to_markdown.convert("<p>Use # and | and ~ in text.</p>", "{\"escapeMisc\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Use") |> should.equal(True)
  result.content |> string.contains("and") |> should.equal(True)
  result.content |> string.contains("in text.") |> should.equal(True)
}

// escape_underscores option escapes underscores in plain text
pub fn options_escape_underscores_test() {
  let result = html_to_markdown.convert("<p>The variable_name is defined.</p>", "{\"escapeUnderscores\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("variable") |> should.equal(True)
  result.content |> string.contains("name") |> should.equal(True)
  result.content |> string.contains("defined.") |> should.equal(True)
}

// Elements matching CSS attribute selector are excluded entirely
pub fn options_exclude_selectors_attribute_test() {
  let result = html_to_markdown.convert("<body><div role=\"complementary\">Sidebar</div><p>Primary text</p></body>", "{\"excludeSelectors\":[\"[role='complementary']\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Primary text") |> should.equal(True)
  result.content |> string.contains("Sidebar") |> should.equal(False)
}

// Elements matching CSS class selector are excluded entirely
pub fn options_exclude_selectors_class_test() {
  let result = html_to_markdown.convert("<body><div class=\"cookie-banner\">Accept cookies</div><p>Main content</p></body>", "{\"excludeSelectors\":[\".cookie-banner\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Main content") |> should.equal(True)
  result.content |> string.contains("cookies") |> should.equal(False)
}

// Empty exclude_selectors list does not affect output
pub fn options_exclude_selectors_empty_noop_test() {
  let result = html_to_markdown.convert("<p>Hello world</p>", "{\"excludeSelectors\":[]}")
  result |> should.be_ok()
  result.content |> string.contains("Hello world") |> should.equal(True)
}

// Elements matching CSS id selector are excluded entirely
pub fn options_exclude_selectors_id_test() {
  let result = html_to_markdown.convert("<body><div id=\"ad-container\">Buy stuff</div><p>Article text</p></body>", "{\"excludeSelectors\":[\"#ad-container\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Article text") |> should.equal(True)
  result.content |> string.contains("Buy stuff") |> should.equal(False)
}

// Multiple CSS selectors each exclude their matched elements
pub fn options_exclude_selectors_multiple_test() {
  let result = html_to_markdown.convert("<body><nav class=\"nav\">Menu</nav><p>Content</p><footer>Footer</footer></body>", "{\"excludeSelectors\":[\".nav\",\"footer\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Content") |> should.equal(True)
  result.content |> string.contains("Menu") |> should.equal(False)
  result.content |> string.contains("Footer") |> should.equal(False)
}

// All descendants of excluded elements are dropped
pub fn options_exclude_selectors_nested_content_dropped_test() {
  let result = html_to_markdown.convert("<body><aside class=\"sidebar\"><h2>Related</h2><p>Sidebar text</p></aside><main><p>Main text</p></main></body>", "{\"excludeSelectors\":[\".sidebar\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Main text") |> should.equal(True)
  result.content |> string.contains("Related") |> should.equal(False)
  result.content |> string.contains("Sidebar text") |> should.equal(False)
}

// Exclude selectors work in plain text output mode
pub fn options_exclude_selectors_plain_text_mode_test() {
  let result = html_to_markdown.convert("<body><div class=\"nav\">Navigation</div><p>Article body</p></body>", "{\"excludeSelectors\":[\".nav\"],\"outputFormat\":\"Plain\"}")
  result |> should.be_ok()
  result.content |> string.contains("Article body") |> should.equal(True)
  result.content |> string.contains("Navigation") |> should.equal(False)
}

// exclude_selectors drops entire subtree unlike strip_tags which keeps children
pub fn options_exclude_selectors_vs_strip_tags_test() {
  let result = html_to_markdown.convert("<body><div class=\"wrapper\"><p>Inner paragraph</p></div><p>Outer text</p></body>", "{\"excludeSelectors\":[\".wrapper\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Outer text") |> should.equal(True)
  result.content |> string.contains("Inner paragraph") |> should.equal(False)
}

// Extract metadata returns document metadata when enabled
pub fn options_extract_metadata_true_test() {
  let result = html_to_markdown.convert("<html><head><title>Test Page</title><meta name='description' content='A test page'></head><body><p>Content</p></body></html>", "{\"extractMetadata\":true}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.metadata.document.title |> should.equal("Test Page")
  result.metadata.document.description |> should.equal("A test page")
}

// ATX heading style produces hash-prefixed headings
pub fn options_heading_style_atx_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><h2>Subtitle</h2>", "{\"headingStyle\":\"Atx\"}")
  result |> should.be_ok()
  result.content |> string.contains("# Title") |> should.equal(True)
  result.content |> string.contains("## Subtitle") |> should.equal(True)
}

// ATX closed heading style adds closing hashes
pub fn options_heading_style_atx_closed_test() {
  let result = html_to_markdown.convert("<h1>Closed Heading</h1>", "{\"headingStyle\":\"AtxClosed\"}")
  result |> should.be_ok()
  result.content |> string.contains("# Closed Heading #") |> should.equal(True)
}

// Underlined heading style produces setext-style headings for h1 and h2
pub fn options_heading_style_underlined_test() {
  let result = html_to_markdown.convert("<h1>Main Title</h1>", "{\"headingStyle\":\"Underlined\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Main Title") |> should.equal(True)
}

// Mark tag rendered as bold
pub fn options_highlight_bold_test() {
  let result = html_to_markdown.convert("<p>Text with <mark>highlighted</mark> text.</p>", "{\"highlightStyle\":\"Bold\"}")
  result |> should.be_ok()
  result.content |> string.contains("**highlighted**") |> should.equal(True)
}

// Mark tag with double equal highlight style
pub fn options_highlight_double_equal_test() {
  let result = html_to_markdown.convert("<p>Text with <mark>highlighted</mark> here.</p>", "{\"highlightStyle\":\"DoubleEqual\"}")
  result |> should.be_ok()
  result.content |> string.contains("==highlighted==") |> should.equal(True)
}

// Mark tag with no highlight style strips the mark
pub fn options_highlight_none_test() {
  let result = html_to_markdown.convert("<p>Text with <mark>plain</mark> content.</p>", "{\"highlightStyle\":\"None\"}")
  result |> should.be_ok()
  result.content |> string.contains("plain") |> should.equal(True)
  result.content |> string.contains("==") |> should.equal(False)
}

// Images inside specified tags stay inline
pub fn options_keep_inline_images_in_paragraph_test() {
  let result = html_to_markdown.convert("<p>Text <img src='icon.png' alt='icon'> more text</p>", "{\"keepInlineImagesIn\":[\"p\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Text") |> should.equal(True)
  result.content |> string.contains("more text") |> should.equal(True)
}

// Links use reference-style formatting
pub fn options_link_style_reference_test() {
  let result = html_to_markdown.convert("<p><a href='https://example.com'>Example</a> and <a href='https://other.com'>Other</a></p>", "{\"linkStyle\":\"Reference\"}")
  result |> should.be_ok()
  result.content |> string.contains("Example") |> should.equal(True)
  result.content |> string.contains("Other") |> should.equal(True)
  result.content |> string.contains("example.com") |> should.equal(True)
}

// Custom bullet character for unordered lists
pub fn options_list_custom_bullets_test() {
  let result = html_to_markdown.convert("<ul><li>Item A</li><li>Item B</li></ul>", "{\"bullets\":\"*\"}")
  result |> should.be_ok()
  result.content |> string.contains("* Item A") |> should.equal(True)
  result.content |> string.contains("* Item B") |> should.equal(True)
}

// Tab indentation type for nested list items
pub fn options_list_indent_tabs_test() {
  let result = html_to_markdown.convert("<ul><li>Parent<ul><li>Child</li></ul></li></ul>", "{\"listIndentType\":\"Tabs\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Parent") |> should.equal(True)
  result.content |> string.contains("Child") |> should.equal(True)
}

// Nested lists indented with 4 spaces per level
pub fn options_list_indent_width_four_test() {
  let result = html_to_markdown.convert("<ul><li>Outer<ul><li>Inner</li></ul></li></ul>", "{\"listIndentWidth\":4}")
  result |> should.be_ok()
  result.content |> string.contains("Outer") |> should.equal(True)
  result.content |> string.contains("Inner") |> should.equal(True)
}

// Default max_depth (null) converts deeply nested content fully
pub fn options_max_depth_default_unlimited_test() {
  let result = html_to_markdown.convert("<div><div><div><div><p>Deep content</p></div></div></div></div>")
  result |> should.be_ok()
  result.content |> string.contains("Deep content") |> should.equal(True)
}

// max_depth truncates content beyond the specified depth
pub fn options_max_depth_truncates_test() {
  let result = html_to_markdown.convert("<div><p>Shallow</p><div><div><div><p>Too deep</p></div></div></div></div>", "{\"maxDepth\":3}")
  result |> should.be_ok()
  result.content |> string.contains("Shallow") |> should.equal(True)
  result.content |> string.contains("Too deep") |> should.equal(False)
}

// max_depth of 0 produces empty output
pub fn options_max_depth_zero_empty_test() {
  let result = html_to_markdown.convert("<p>Hello</p>", "{\"maxDepth\":0}")
  result |> should.be_ok()
  result.content |> should.equal("")
}

// Hard line breaks rendered with backslash
pub fn options_newline_backslash_test() {
  let result = html_to_markdown.convert("<p>Line one<br>Line two</p>", "{\"newlineStyle\":\"Backslash\"}")
  result |> should.be_ok()
  result.content |> string.contains("Line one") |> should.equal(True)
  result.content |> string.contains("Line two") |> should.equal(True)
}

// Hard line breaks rendered with trailing spaces
pub fn options_newline_spaces_test() {
  let result = html_to_markdown.convert("<p>First<br>Second</p>", "{\"newlineStyle\":\"Spaces\"}")
  result |> should.be_ok()
  result.content |> string.contains("First") |> should.equal(True)
  result.content |> string.contains("Second") |> should.equal(True)
}

// Djot output format produces djot-compatible markup
pub fn options_output_format_djot_test() {
  let result = html_to_markdown.convert("<p>Simple paragraph.</p>", "{\"outputFormat\":\"Djot\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Simple paragraph.") |> should.equal(True)
}

// Default markdown output format produces standard markdown
pub fn options_output_format_markdown_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><p>Some text.</p>", "{\"headingStyle\":\"Atx\",\"outputFormat\":\"Markdown\"}")
  result |> should.be_ok()
  result.content |> string.contains("# Title") |> should.equal(True)
  result.content |> string.contains("Some text.") |> should.equal(True)
}

// Plain text output format strips markdown syntax
pub fn options_output_format_plain_test() {
  let result = html_to_markdown.convert("<h1>Title</h1><p>Some <strong>bold</strong> text.</p>", "{\"outputFormat\":\"Plain\"}")
  result |> should.be_ok()
  result.content |> string.contains("Title") |> should.equal(True)
  result.content |> string.contains("bold") |> should.equal(True)
  result.content |> string.contains("text.") |> should.equal(True)
}

// Aggressive preset removes nav, footer, aside unconditionally
pub fn options_preprocessing_aggressive_test() {
  let result = html_to_markdown.convert("<nav>Menu</nav><article><h1>Title</h1><p>Content</p></article><aside>Sidebar</aside><footer>Footer</footer>", "{\"preprocessing\":{\"preset\":\"Aggressive\"}}")
  result |> should.be_ok()
  result.content |> string.contains("Title") |> should.equal(True)
  result.content |> string.contains("Content") |> should.equal(True)
  result.content |> string.contains("Menu") |> should.equal(False)
}

// Minimal preset preserves nav, footer, aside
pub fn options_preprocessing_minimal_test() {
  let result = html_to_markdown.convert("<nav>Navigation</nav><p>Content</p><footer>Footer</footer>", "{\"preprocessing\":{\"preset\":\"Minimal\"}}")
  result |> should.be_ok()
  result.content |> string.contains("Navigation") |> should.equal(True)
  result.content |> string.contains("Content") |> should.equal(True)
  result.content |> string.contains("Footer") |> should.equal(True)
}

// Forms are removed when remove_forms is true
pub fn options_preprocessing_remove_forms_test() {
  let result = html_to_markdown.convert("<p>Before</p><form><input type='text'/><button>Submit</button></form><p>After</p>", "{\"preprocessing\":{\"removeForms\":true}}")
  result |> should.be_ok()
  result.content |> string.contains("Before") |> should.equal(True)
  result.content |> string.contains("After") |> should.equal(True)
  result.content |> string.contains("Submit") |> should.equal(False)
}

// Iframe tags preserved as raw HTML in output
pub fn options_preserve_tags_iframe_test() {
  let result = html_to_markdown.convert("<p>Before</p><iframe src='video.html' width='560'></iframe><p>After</p>", "{\"preserveTags\":[\"iframe\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Before") |> should.equal(True)
  result.content |> string.contains("After") |> should.equal(True)
  result.content |> string.contains("<iframe") |> should.equal(True)
}

// Images are omitted from output when skipImages is true
pub fn options_skip_images_true_test() {
  let result = html_to_markdown.convert("<p>Before <img src='test.jpg' alt='photo'> After</p>", "{\"skipImages\":true}")
  result |> should.be_ok()
  result.content |> string.contains("Before") |> should.equal(True)
  result.content |> string.contains("After") |> should.equal(True)
  result.content |> string.contains("photo") |> should.equal(False)
}

// Strip newlines produces single-line paragraphs
pub fn options_strip_newlines_test() {
  let result = html_to_markdown.convert("<p>First paragraph.</p><p>Second paragraph.</p>", "{\"stripNewlines\":true}")
  result |> should.be_ok()
  result.content |> string.contains("First paragraph.") |> should.equal(True)
  result.content |> string.contains("Second paragraph.") |> should.equal(True)
}

// Div and span tags stripped but content preserved
pub fn options_strip_tags_div_span_test() {
  let result = html_to_markdown.convert("<div class='wrapper'><p>Inside div</p></div><p>Outside <span class='hl'>span text</span></p>", "{\"stripTags\":[\"div\",\"span\"]}")
  result |> should.be_ok()
  result.content |> string.contains("Inside div") |> should.equal(True)
  result.content |> string.contains("span text") |> should.equal(True)
}

// Strong and em tags use underscore symbol instead of asterisk
pub fn options_strong_em_underscore_test() {
  let result = html_to_markdown.convert("<p><strong>bold</strong> and <em>italic</em></p>", "{\"strongEmSymbol\":\"_\"}")
  result |> should.be_ok()
  result.content |> string.contains("__bold__") |> should.equal(True)
  result.content |> string.contains("_italic_") |> should.equal(True)
}

// Subscript rendered with tilde symbol
pub fn options_sub_symbol_tilde_test() {
  let result = html_to_markdown.convert("<p>H<sub>2</sub>O</p>", "{\"subSymbol\":\"~\"}")
  result |> should.be_ok()
  result.content |> string.contains("~2~") |> should.equal(True)
}

// Superscript rendered with caret symbol
pub fn options_sup_symbol_caret_test() {
  let result = html_to_markdown.convert("<p>x<sup>2</sup></p>", "{\"supSymbol\":\"^\"}")
  result |> should.be_ok()
  result.content |> string.contains("^2^") |> should.equal(True)
}

// Normalized whitespace mode collapses multiple spaces
pub fn options_whitespace_normalized_test() {
  let result = html_to_markdown.convert("<p>Text   with    extra   spaces.</p>", "{\"whitespaceMode\":\"Normalized\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Text") |> should.equal(True)
  result.content |> string.contains("with") |> should.equal(True)
  result.content |> string.contains("extra") |> should.equal(True)
  result.content |> string.contains("spaces.") |> should.equal(True)
}

// Strict whitespace mode preserves whitespace as-is
pub fn options_whitespace_strict_test() {
  let result = html_to_markdown.convert("<p>Preserved   spacing.</p>", "{\"whitespaceMode\":\"Strict\"}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("Preserved") |> should.equal(True)
  result.content |> string.contains("spacing.") |> should.equal(True)
}

// Wrap option disabled preserves long lines without breaking
pub fn options_wrap_disabled_test() {
  let result = html_to_markdown.convert("<p>This is a long paragraph that should not be wrapped at all because wrapping is disabled.</p>", "{\"wrap\":false}")
  result |> should.be_ok()
  result.content |> string.contains("This is a long paragraph that should not be wrapped at all because wrapping is disabled.") |> should.equal(True)
}

// Wrap option enabled with custom width wraps long lines
pub fn options_wrap_enabled_test() {
  let result = html_to_markdown.convert("<p>This is a long paragraph that should be wrapped at the specified column width when the wrap option is enabled.</p>", "{\"wrap\":true,\"wrapWidth\":40}")
  result |> should.be_ok()
  result.content |> list.is_empty |> should.equal(False)
  result.content |> string.contains("This is a long paragraph") |> should.equal(True)
}

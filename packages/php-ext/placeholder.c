#include "php.h"

/* This placeholder file allows the Windows phpize tooling to register the
 * extension. The build rules replace the resulting DLL with the Rust-produced
 * artifact, so no symbols from this stub are ever used at runtime. */

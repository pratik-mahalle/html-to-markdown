// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: result. */
class ResultTest {
    @Test
    fun testResultTablesEmptyWhenNoTables() {
        // Result tables array is empty when input has no tables
        val result = HtmlToMarkdownRs.convert("<p>No tables here</p>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultTablesMultiple() {
        // Multiple tables each appear in the tables array
        val result = HtmlToMarkdownRs.convert("<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", "{\"includeDocumentStructure\":true}")
        assertTrue(result.tables.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testResultTablesSimple() {
        // Simple table populates the tables array in result
        val result = HtmlToMarkdownRs.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", "{\"includeDocumentStructure\":true}")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultTablesWithoutStructureFlag() {
        // Tables array is empty when includeDocumentStructure is false
        val result = HtmlToMarkdownRs.convert("<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForCleanInput() {
        // Warnings array is empty for well-formed HTML without problematic content
        val result = HtmlToMarkdownRs.convert("<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForComplexInput() {
        // Warnings array is empty for complex but valid HTML
        val result = HtmlToMarkdownRs.convert("<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForMalformedHtml() {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        val result = HtmlToMarkdownRs.convert("<p>Unclosed paragraph<div>Mixed nesting</p></div>")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

}

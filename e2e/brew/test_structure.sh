#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f56b01d524e0f793a60fa854cfd8573e3b55a09c5404694c86f0be39a2e01847
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: structure
set -euo pipefail

test_structure_code_block() {
  # Fenced code block produces Code node
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 2 'document.nodes'
}

test_structure_deep_nesting_h1_h2_h3() {
  # H1 > H2 > H3 creates three levels of heading nesting
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 5 'document.nodes'
}

test_structure_h1_h2_nested_group() {
  # H1 followed by H2 creates a nested group under the H1
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 3 'document.nodes'
}

test_structure_heading_paragraph() {
  # Simple heading followed by paragraph produces Heading and Paragraph nodes
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 2 'document.nodes'
}

test_structure_list() {
  # Unordered list produces List and ListItem nodes
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 2 'document.nodes'
}

test_structure_multiple_headings() {
  # Multiple headings create multiple Heading nodes with correct levels
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 4 'document.nodes'
}

test_structure_sibling_h1_groups() {
  # H1, H2, then another H1 creates two sibling top-level groups
  local output
  output=$(html_to_markdown convert)

  local val_content
  val_content=$(echo "$output" | jq -r '.content')
  assert_not_empty "$val_content" 'content'
  local val_document_nodes
  val_document_nodes=$(echo "$output" | jq -r '.document.nodes')
  assert_not_empty "$val_document_nodes" 'document.nodes'
  local count_document_nodes
  count_document_nodes=$(echo "$output" | jq '.document.nodes | length')
  assert_count_min "$count_document_nodes" 4 'document.nodes'
}

run_tests_structure() {
  run_test test_structure_code_block
  run_test test_structure_deep_nesting_h1_h2_h3
  run_test test_structure_h1_h2_nested_group
  run_test test_structure_heading_paragraph
  run_test test_structure_list
  run_test test_structure_multiple_headings
  run_test test_structure_sibling_h1_groups
}
